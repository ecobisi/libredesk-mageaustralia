# v1.0.3 → v2.1.1-plus-enhancements port — design

**Status:** approved 2026-04-27
**Branch:** `v2.1.1-plus-enhancements` on the `mageaustralia/libredesk` fork
**Owner:** Matthew Campbell (with Claude as the primary porter)

## 1. Goals & non-goals

**Goal:** bring the new v2.1.1-based fork branch to feature parity with the production `v1.0.3-plus-enhancements` branch running on `desk.tenniswarehouse.com.au`. End state: the new branch supports every workflow the live branch does, including the AI/RAG/ecommerce/voicemail/PCI/mobile-push features that depend on host config.

**Non-goals:**
- Re-litigating product decisions baked into v1.0.3 features.
- Improving v1.0.3 features beyond what's already there. Polish that emerges naturally from rule-C grouping is fine; refactor-for-its-own-sake is not.
- Submitting upstream PRs unless a specific feature is flagged as worth contributing back. PR scope is decided at port time, not in this spec.

## 2. Rules of the game

**Unit of work:** one commit per coherent feature. "Coherent" is judgement-based, leaning toward upstream's natural feature boundaries:

- Where upstream bundled unrelated work into one commit (e.g. `82e08d45` covers PCI + hover preview + quoted thread + lightbox nav), split into separate commits.
- Where upstream split a feature across an initial implementation plus several fix commits (e.g. forward feature + 4 follow-up CC/BCC/colour/inline-image fixes), bundle into a single current-state commit.

**Source-of-truth scope:** for each feature, run `git log --all --grep` on the live `/home/ubuntu/libredesk` repo to find every commit referencing that feature, including bug-fix and refinement commits. The port commit reflects the feature's *current* working state on v1.0.3, not its initial buggy implementation.

**Order:** chronological-ish through upstream v1.0.3 history. Tier 3 features get ported at their natural chronological position — they don't get bumped to the back just because they need host config. Config-gating notes attached where needed.

**Skip rules:** anything already on `v2.1.1-plus-enhancements` (see Section 5 status column) is skipped. Pure-cosmetic deploy auto-commits are skipped. Pure docs/README commits are skipped unless the docs are user-facing (e.g. a settings-page help link).

## 3. Execution loop per feature

```
1. ssh to live, git log --all --grep="<keywords>" → list source commits
2. git show on each → understand the diff and any later fixes
3. read v2 destination files → identify adaptation deltas
   (flat IncomingMessage, frontend/apps/main/src/, @shared-ui imports, etc.)
4. apply changes, adapting per v2 conventions
5. go build ./... + go test ./<changed packages>/...
6. spawn code-reviewer agent on the diff
7. apply must-fix items inline; defer cosmetic suggestions
8. commit with full why-context message
9. update Section 5 status: pending → done <hash>
10. checkpoint to shared memory if session is long (per CLAUDE.md)
```

Each commit ends in a buildable, tested, reviewed state — so we can stop at any commit boundary and resume cleanly.

**Stopping points:** end of every feature is a stop point. End of every "session" (chosen by Matthew, not Claude) gets pushed to `mageaustralia` so nothing's only on the local Mac.

**Failure protocol:**
- Build fails → fix it before the commit.
- Test fails → same.
- Code-reviewer flags a Critical → must-fix before commit.
- Code-reviewer flags an Important → fix unless the rationale to defer is recorded in the commit message.
- Suggestion → ignore unless cheap.

## 4. Tier 3 host-config requirements

Tier 3 features ship code that depends on host config (API keys, external service URLs). All four are designed to ship dark — present in the binary, inert without config — so we can land the code without blocking on config.

### 4.1 RAG / OpenRouter / multimodal AI
- **Config:** AI provider (OpenAI / Claude / OpenRouter) configured via `/admin/ai`. API key encrypted in the `ai_providers` table. Knowledge sources configured via `/admin/knowledge-sources`.
- **Degrades when absent:** "Generate Response" button hidden in `ReplyBox.vue`. Background RAG sync coordinator logs `no provider configured` and exits.
- **Already on live:** yes (in daily use by Tennis Warehouse helpdesk agents).

### 4.2 Ecommerce (Maho/Magento)
- **Config:** OAuth2 `client_id` + `client_secret` + base URL configured via `/admin/ecommerce`. Per-inbox enable toggle.
- **Degrades when absent:** "+ Orders" button hidden in `ReplyBoxMenuBar.vue`. Multi-stage context-gathering pipeline returns empty.
- **Already on live:** yes.

### 4.3 Voicemail transcription
- **Config:** Whisper API key + Twilio webhook secret. Audio storage backend (filesystem or S3 — same as media). Per-inbox transcription toggle.
- **Degrades when absent:** background transcribe-worker logs `no transcriber configured` and skips. Audio messages still play in the UI; just no transcript.
- **Already on live:** yes (Spinfire helpdesk).

### 4.4 PCI redaction (`go-pci-scrub`)
- **Config:** none — purely in-process. Per-inbox skip-PCI-scan toggle for cases where redaction interferes (e.g. a payment-gateway helpdesk that legitimately discusses card numbers).
- **Degrades when absent:** N/A — always on by default. Per-inbox skip toggle defaults to off.
- **Already on live:** yes (TW used as compliance buffer).

### 4.5 Mobile push (FCM + Google mobile auth)
- **Config:** Firebase service-account JSON at `/libredesk/firebase-service-account.json` inside the Docker container. Mounted from host. Local copy already exists at `/Volumes/second_disk/Development/libredesk-staging/firebase-service-account.json` (gitignored when the port lands).
- **Degrades when absent:** `NewFCMSender` returns `nil`; dispatcher's FCM hook becomes a no-op. In-app notifications still work over WebSocket.
- **Already on live:** yes (Flutter app dependency).

## 5. Ordered work list

Per Section 2, units are organised chronologically by theme. Each unit lists its v1.0.3 source commit hashes (run `git show <hash>` on live to inspect), an effort estimate (S = ~30 min, M = ~1–2h, L = ~3–5h, XL = day-long), and dependencies on other units. Status starts `pending`; gets updated to `done <hash on enhancements branch>` after the port commit lands.

Already-ported features are in Section 5.0 for reference.

### 5.0 Already on `v2.1.1-plus-enhancements` (skip)

| Feature | Hash on enhancements branch |
|---------|------|
| Bulk actions on conversations | `18045961` |
| Table view + column resize basics | `20eb7d1b`, `6abfd4f8` |
| Inline images, lightbox, attachment UX | `ff8b2223` |
| Edit and soft-delete private notes (+ PR fixes) | `ccd17580`, `3d7465a3` |
| Theme switcher with discovery | `4f0ecb9e` |
| Fresh theme | `3508730c` |
| Forward email to external recipient | `e393a65d` |
| Smart team reassign | `c94245d8` |
| Multi-status conversation list filter | `3f764f2e` |
| Auto-assign on reply (per-inbox toggle) | `f43905db` |
| DMARC sender detection (incl. contact-form parsing, BCC self-skip) | `cf6e6081` |
| Spam & Trash + auto-cleanup + IMAP folder threading | `d71303f6` |
| Customer reply in-app notification | `e701f228` |

### 5.1 Tier 1 — feature-parity essentials

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T1a | Inline quick-assign dropdowns on conv list (agent + team chips per row) | `fe5fefb7` | M | – | done d6669251 | Already-bundled with bulk-actions in upstream — pull just the per-row dropdown bits. |
| T1b | SMTP/IMAP connection testing | `870da3d9` (notification SMTP), `f0374171` (per-inbox IMAP+SMTP with debug logs) | M | – | done bb258236 | Two sub-screens: notification settings test button, inbox-edit test panel. |
| T1c | Agent collision detection (presence + reply-collision warning) | subset of `9dc758c6` | M | – | done 93465230 | WebSocket presence + warning banner when another agent is composing. |
| T1d | Ticket merging | subset of `9dc758c6`, `d4f953b1` (merge-by-ref portion), `50933de0`, `d9aaccc9`, `8128675e`, `34471e3d` | L | – | done a3370004 | Initial feature + reopen-on-merge fix + banner styling fix + WS broadcast fix + merge ordering shift. Chore: removed dead ReopenConversationByID prepared stmt (991d7654). Code-review fixes (a3370004): by-ref lookup endpoint (C1), mention/notification move in tx (I1), partial-failure warnings (I4), WS ordering comment (I3), rename shadowing t→tk (S1), 50-secondary cap (S3). Plan called for a "Merged from #X" banner in MessageList — descoped because v1.0.3 doesn't actually have this banner; only the destination "Merged into" banner exists. Faithful port. |

### 5.2 Email composer / reply UX

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| EC1 | Send & Set Status dropdown + delete draft button + status chevron | `693974ee` | M | – | done 3b92d39c | |
| EC2 | Send-fail error toast + restore editor content | `d94cd572` | S | – | done 6d87e48a | |
| EC3 | Undo send with countdown | subset of `fdfcf50c` | M | – | done 641a8006 | Split bundled commit. Code-review fix in 641a8006 (was 807d6d0c). |
| EC4 | Sticky subject header bar | subset of `fdfcf50c` | S | – | done 856eb6f5 | |
| EC5 | Inline subject editing in sticky header | `3de2f5bd` | M | EC4 | done 856eb6f5 | |
| EC6 | Fullscreen reply editor + sidebar toggle | `5097e66e` | M | – | done 304bea50 | v2 already had the Dialog scaffold + ReplyBoxContent toggle wired; port resized the overlay to 92%×88% (Freshdesk-like) and added a SidebarTrigger to InboxLayout's table-view back-bar so the global nav can be collapsed when reading a thread. Per-session state (no useStorage). Sticky subject (T2d) + undo banner (T2c) are obscured by the Radix portal backdrop — no teleport needed; documented trade-off that the 5s undo affordance is unreachable while fullscreen is open (ESC to exit restores it). |
| EC7 | Gmail-style TO/CC/BCC composer + scroll-to-last | `9bcae060` | M | – | done 765d8490 | scroll-to-last half of the source commit was a fresh-theme nested-scroll fix; v2's MessageList owns its own scroll container, so that half is N/A. CC/BCC plumbed through createConversationRequest → QueueReply. First TO email is primary contact (parity with v1.0.3 — additional TO chips accepted in UI but backend consumes only first). |
| EC8 | Per-email remove buttons on TO/CC/BCC | `17579887` | S | – | done 765d8490 | EmailTagInput on ReplyBoxContent. Backed by comma-joined string so existing validateEmails/parseTo/parseCC/parseBCC helpers stay untouched. |
| EC9 | Clear (X) button on CC/BCC fields | `224c1668` | S | – | done 765d8490 | Superseded by EC8 — chip-level remove is finer-grained, so the per-field clear-X was dropped instead of ported. No separate code change. |
| EC10 | Cursor position to start of editor on new conv/reply | `0124d3a1` | S | – | done 765d8490 | TipTap `autofocus: 'start'` + commands.focus('start') in the htmlContent watcher. TextEditor.focus() / ReplyBoxContent.focus() default to 'start', so T2c's conv-switch focus call also lands cursor at top. |
| EC11 | Paste/drop in TipTap editor fix | `0934c57b` | S | – | done f3e5bf8a | Already in v2 from the inline-image bundle — `handlePaste` / `handleDrop` are sync with fire-and-forget upload promises. T2g verified, no additional code change. |
| EC12 | Exit list on double-Enter + formatting toolbar | `93766088` | M | – | done f3e5bf8a | TextEditor exposes `runCommand()` + adds `liftListItem` fallback in `handleKeyDown` for empty-list-item Enter. ReplyBoxMenuBar swaps the Smile button for an "Aa" toggle that reveals Bold / Italic / lists / Link / Image / Emoji. Image button uses a hidden file input mirroring the v1.0.3 path. |
| EC13 | Agent name in email From header | `fd21e08b` | S | – | done 4c386b0d + fix fd224363 | T2h. Skipped for forwards / system user / when From already has display name. Lookup failures fall through silently. Code-review fixups (S1 invariant comment, S2 redundant ToLower drop) folded into fd224363. |
| EC14 | From switcher in reply (per-inbox aliases) | subset of `97533510` | M | – | done 4c386b0d + fix fd224363 | T2h. Aliases stored in `inboxes.config.aliases` JSONB array (no schema change beyond the v2.2.2 registration stub). Per-message override via `meta.from`, server-side validated against primary + aliases. Absorbs MP2 per section 5.5; no upstream MP dependency. fd224363 adds `req.From` to `checkMessageDedup` key (I1 — alias-switched resend was being 409'd) and dedupes `fromOptions` against admin typos (S3). |
| EC15 | Macro toolbar (Zap) button for quick macro access | subset of `97533510` | S | – | done 665dfd95 | T2i. Zap button added to ReplyBoxMenuBar alongside the Aa formatting toggle (top-level, not inside the formatting toolbar — macros are an editor-level action, not typography). Reuses the existing `SET_NESTED_COMMAND` emitter the Ctrl+K flow already listens on. New `macroPickerCommand` prop lets CreateConversation target `apply-macro-to-new-conversation` instead of the existing-conversation flow. |
| EC16 | Smart new-conversation defaults | subset of `c7b60817` | S | – | done 665dfd95 | T2i. Pre-fills `agent_id` with the current user when the new-conversation dialog opens. Adaptation delta vs v1.0.3: source commit also auto-selected the agent's team default inbox via `team.default_inbox_id`, but v2 has no `default_inbox_id` column on the team model (data-migration not ported), so the inbox-defaulting half is dropped. Existing T2h auto-select-first-inbox still picks a sensible inbox. |
| EC17 | Send & Set Status dedup guard | subset of `fdfcf50c` | S | EC1 | done 3b92d39c | Bundled with EC1 — same commit. Dedup map ported with EC17's set_status-in-key enhancement. |
| EC18 | Draft clearing fix on conv switch | subset of `fdfcf50c` | S | – | done 641a8006 | Code-review fix in 641a8006 (was 807d6d0c). |
| EC19 | Remove signature from private notes, restore on switch to reply | `a9f77c6d` | S | MP1 | done 124bfb05 | Implemented as an extension to the existing forward-reset watch on `messageType` in ReplyBox.vue. Insert path now wraps the inserted signature with a `<!-- sig -->` HTML comment marker so the watcher can locate and excise it on switch to private_note, then re-insert on switch back. Marker is invisible to email clients (HTML comment) and the editor. Switch-back reuses the original spacing if the editor was empty, else appends below whatever the agent typed. |
| EC20 | Submit & Set Status on new conversations (mirror of EC1 for the new-conversation dialog) | `a427efc2` + refinement `278d3bdd` | S | EC1 | done 7a1a733c | `CreateConversation.vue` gets a Submit + chevron split-button mirroring EC1's Send & Set Status pattern. Status list is **dynamic** — pulled from the same `conversationStore.statuses`, filtered by the same `category in (open, resolved)` predicate EC1 uses (one source of truth for both pickers). v2 has no `show_on_send` field so the spec's original wording is adapted to the v2 category model. Open + Snoozed excluded (Open is default; Snoozed needs a duration we don't collect). UX24 absorbed: dropdown content has `max-h-[60vh] overflow-y-auto` for installs with many statuses. Backend adds `set_status` field on `createConversationRequest`; after `handleCreateConversation` inserts the convo, calls `UpdateConversationStatus` with the requested status. Manager validates the name internally — backend rejects Open + Snoozed up-front. Failures log a warning but don't fail the create. Self-review caught & fixed: `pendingSubmitStatus` could survive a validation failure and silently ride along with the next plain-Submit click — now reset in submitWithStatus's finally + re-snapshot-reset in handleSubmit's success path. |

### 5.3 Email rendering & threading

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| ER1 | Strip quoted thread content on storage (prevent exponential message growth) | `96289a2f` | M | – | done dba01341 | InsertMessage strips post-`<!-- thread -->` (outgoing) + first `<blockquote>` within first 50KB of >50KB content (incoming). TextContent (search) keeps the full body. |
| ER2 | Outlook quote collapse | subset of `6e2a8d2f` | M | – | done 7740d528 | MessageBubble's `hasQuotedContent` extended to detect `gmail_quote` and `divRplyFwdMsg` (Outlook). `.hide-quoted-text` SCSS rule expanded to hide `.gmail_quote`, `.gmail_attr`, `[class*="gmail_quote"]`, `#divRplyFwdMsg`, `[id*="divRplyFwdMsg"]` so the toggle actually collapses Outlook + Gmail-class quoted threads. The `quotedThreadHtml` undo-restore split from the same source commit was NOT ported — v2's undo restores `htmlContent` as one blob and that's working; introducing a separate quoted-thread state would be a deeper restructure. |
| ER3 | Subject threading | subset of `6e2a8d2f` | M | – | done 7740d528 | New `find-conversation-by-subject-contact` query + `FindConversationBySubjectContact` prepared statement. `findOrCreateConversation` now falls back to (contact + inbox + Re/Fw-stripped lowercase subject) match when the In-Reply-To/References lookup returns nothing AND the email has threading headers. Excludes status_id IN (3,4,5,6) so Resolved/Closed/Spam/Trashed don't get re-opened. Header gate prevents automated emails (payment notifications etc.) with shared subjects from collapsing into old conversations. |
| ER4 | Strip signed-URL stacking in forwarded/quoted content | `a1fefcb1` | S | – | done 5dde9ee0 | Already done by `uploadsURLForEmailRe` regex in commit 5dde9ee0 — the trailing `(?:\?[^"\s]*)?` group consumes any existing `?sig=…&exp=…` so each rewrite replaces rather than stacks. |
| ER5 | Signed URLs for inline images in notification emails | subset of `d4f953b1` | S | – | done 5dde9ee0 | `makeAbsoluteURLs` + `GetEmailURL` (30-day signed URL) wired into notifyParticipants in commit 5dde9ee0. Uses long-expiry variant (vs v1.0.3's `GetSignedURL`) so Gmail/etc. proxy fetches days later still resolve. |
| ER6 | Email HTML rendering tightening (whitespace gaps, layout) | `797b8617` | S | – | done 7740d528 | Dropped `whitespace-pre-wrap` from the HTML email branch in MessageBubble; SCSS adds `.native-html { white-space: normal !important }`, `h1-h6 { margin: 0.25em 0 }`, `p { margin: 0.15em 0 }`, swaps `* { max-width }` → `* { width }`. `sanitizedContent` strips empty `<p>&nbsp;</p>`/whitespace-only paragraphs from HTML messages. |
| ER7 | Non-image CID inline rendering | subset of `13c8244e` | S | – | done (already in v2) | `replaceCIDInContent` already exists at message.go:1331 and is called from `uploadMessageAttachments` for both already-uploaded and freshly-uploaded paths. Came in via T1's earlier image-attachment work (task #24 — "Fix HTML safety in replaceCIDInContent"). |
| ER8 | Private note edit reactivity in message list | subset of `6e2a8d2f` | S | – | done (already in v2) | v2 routes private-note edits through `mergeMessageUpdate` in stores/conversation.js, which calls `messageCache.updateMessage` + `incrementMessageVersion()`. The reactivity bug v1.0.3 fixed (had to refresh to see edits) doesn't repro in v2 — different store wiring solves the same problem. |
| ER9 | Sender name when multiple participants involved | upstream `879c626f` | S | – | done (already in v2) | v2's MessageBubble already pulls `getFullName`/`getAvatar`/`avatarFallback` from `props.message.author` (per-message data), not the conversation's contact. Same outcome as v1.0.3's participant-lookup fix, different mechanism. |

### 5.4 Filters / search / list UX

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| FS1 | Filter pill bar + favicon + bulk action fixes | `448abe2b` | M | – | done 1ddd9425 (favicon) + 2e7255af (pill bar) | Split into 2 commits: favicon assets/routes (1ddd9425), then pill bar wiring (2e7255af). "Bulk action fixes" half of source commit was layout-only adjustments to make the bulk toolbar coexist with the new pill row — v2's bulk toolbar already coexists fine, no porting needed. ConversationTableView table-fixed fix already in v2. dbutil not_in/relative_date/in_or_null already in v2 (FS2). Port-time fix: tag "exclude" operator now correctly emits "not contains" (with space) instead of v1.0.3's "not_contains" — the underscore form never worked end-to-end on v1.0.3 because internal/conversation/conversation.go only switches on the space form. Latent bug, fixed in port. Backend conversationsAllowedFields gained closed_at + resolved_at so the new date-field options reach the SQL builder. |
| FS2 | View filter operators: in/not_in/in_or_null | `61972439` | M | – | done 5eb1b8b2 | Saved-view path covered (`internal/dbutil/builder.go`): added `not_in`, `in_or_null`, `relative_date`, made `in` accept numeric arrays. Automation evaluator + frontend operator dropdown left untouched — only what saved views needed. Re-open if automation rules need the same operators. |
| FS3 | Per-view filter persistence + dynamic `current_user` in shared views | `e5e14f64` | M | – | done 5eb1b8b2 + 5f1aacf7 | Backend `$current_user` substitution shipped in 5eb1b8b2. Persistence half (5f1aacf7): `useStorage('viewFilters', {})` keyed by listType / `team:<id>` / `view:<id>`. New InboxView wrapper `applyFiltersForView` saves the outgoing view's state, restores the incoming view's saved state, or falls back to existing defaults (Open status for normal views, no status for spam/trash/saved views — those filter server-side). Adapted from v1.0.3's raw-localStorage version to `useStorage` for cross-tab sync; key is camelCase / unprefixed per repo convention. ConversationList's local FilterBar mirror gains a watch on `conversationStore.conversations.adHocFilters` so restored pills render on view-switch (the mirror was uni-directional, write-from-FilterBar only). `(Current user)` option prepended to `assigned_user_id` in the saved-view filter map (`useConversationFilters` `conversationsListFilters`) — pairs with the backend substitution so one saved view ("my open tickets") works for every agent. New i18n key `filter.value.currentUser`. Setters skip persistence when `fetch=false` so the InboxView wrapper owns view-switch persistence and the outgoing view's saved state can't be clobbered mid-transition (small bug latent in the v1.0.3 source). |
| FS4 | Re-fetch list with restored filters on back navigation | `4e90077c` | S | – | done d412219c | FS3 ported the persistence map but not the back-navigation watcher (v1.0.3 had it pre-FS4 from earlier per-view filter work — that part was missed during the FS3 port). FS4 adds both halves at once: the route.name watcher (entering-conversation oldName -> list-route newName) AND the resetAndRefetch the source commit was specifically about. Adapted to v2 conventions: inlined `resetConversations()` + `reFetchConversationsList()` at the call site (matches v2's existing setListStatus/toggleListStatus pattern; v1.0.3 had a bespoke resetAndRefetch helper that v2 doesn't need) and exported `reFetchConversationsList` from the store (was internal-only). Used `CONVERSATION_LIST_TYPE.ASSIGNED` for the fallback instead of the bare 'assigned' string. |
| FS5 | Unassigned filter persistence + correct application | `34c863bd`, `549adec4` | S | – | done 789656e2 | Two-half port. Backend (549adec4): `UnassignedConversations` SQL drops the `assigned_team_id IS NULL` condition so team-routed-but-unpicked-up tickets surface. `TeamUnassignedConversations` keeps its team gate untouched, so only the global Unassigned bucket relaxes. Frontend (34c863bd): the empty-value `validAdHoc` guard in `stores/conversation.js` and the `meaningful` filter in `ConversationList.vue` both exempt `set` / `not set` operators (those are valueless by design, mapping to IS NOT NULL / IS NULL in `dbutil/builder.go`). v1.0.3 also patched `ConversationFilterPanel.vue:syncFromStore`, but v2 uses `FilterBar` / `FilterPill` / `FilterMultiSelect` which re-init from `modelValue` on every popover open, so that restoration bug isn't present in v2. Today's `FilterMultiSelect` only emits `in` / `not_in` / `contains` / `not contains`, so the user can't yet construct a `not set` pill from the pill UI directly. The validation fix is defensive: a saved view's filter (which can carry `not set`) restored into `adHocFilters` would otherwise be silently dropped. |
| FS6 | Subject hover preview | subset of `832ed895`, subset of `82e08d45` | M | – | done 5e6f40e4 | Tooltip on subject (table) + message preview (card) showing Original message + Latest reply. Backend adds `first_message` to `get-conversations` via correlated subquery filtering `type != 'activity'` (the broadened 82e08d45 form, so agent-initiated conversations also surface). Adapted to v2 conventions: shared-ui Tooltip in both views (v1.0.3 used a hand-rolled hover popover for the card view). Card view uses `last_message` (consistency with the always-visible preview line); table view uses `last_interaction` (skips activity entries from being the "latest reply"). Tooltip is suppressed in the card view when a draft banner is showing. 3 new i18n keys under `conversation.list.preview.*`. |
| FS7 | From/To automation filter operators | subset of `832ed895` | M | FS2 | done 27b29f1a | New `inbox_email` automation field constant + evaluator case (returns `conversation.InboxMail`, which the GetConversation path has already stripped of display-name wrapper). Frontend `newConversationFilters` gains an `inbox_email` entry; both labels use new i18n keys (`automation.field.fromEmail` / `toEmail`) instead of v1.0.3's hardcoded English. Subject hover preview half of source commit (FS6) and email rendering fix (out of scope) excluded. |
| FS8 | Search UX improvements (unified search, macros, etc.) | `331d9221`, `c89f023a` | M | – | done f89d7b82 | Bundle two source commits per rule §2. New `GET /api/v1/search` (perm `conversations:read`) backed by `search-unified` query: single SELECT across ref number / subject / contact email / message text content with `DISTINCT ON (c.id)` + `COUNT(*) OVER()` for pagination, returns most-relevant snippet per conversation. Frontend collapses tabs to one flat list with "Load more" + sessionStorage persistence (also doubles for FS11's persist-across-nav half — only the status-change-list-removal half remains for FS12). 331d9221's file-attachment redesign already in v2 from the inline-image bundle; "Conversations → Tickets" / "Messages default tab" tab-renames obsoleted by c89f023a's tab removal; InboxLayout back-to-search behaviour already generalised via router history in v2. Macro improvements: CommandBox routes via nestedCommand OR new `isNewConversationOpen` store flag; ReplyBox delegates to `ReplyBoxContent.insertMacro()` (cursor-position insert + `{{contact.x}}` placeholder substitution); TextEditor auto-focuses before insertContent so unfocused-editor + macro-pick path stops silent-failing. Backend NotifyAssignment exposes `.Message.Content` / `.Message.TextContent` to the assignment notification template (best-effort). Sidebar z-index 10 → 20 to overlay the table sticky header (widget app doesn't consume Sidebar so safe). ConversationTableView gets `min-w-[900px]`. Self-review fix: SearchView defensively coerces older-format `{messages, tickets}` sessionStorage payloads to `[]` so mid-tab upgraders don't see a silently-broken iterator. |
| FS9 | Search crash on null results | `1a44a07f` | S | – | done (covered by FS8 f89d7b82) | v1.0.3 fix added `\|\| []` to `convResults.data.data` and renamed `results.value.conversations.length` → `results.value.tickets.length` in `frontend/src/views/search/SearchView.vue`. Both crash paths are structurally absent in v2 post-FS8: (a) the two-tab `{tickets, messages}` object shape was replaced with a flat `results` array (`frontend/apps/main/src/views/search/SearchView.vue:91`), so `results.value.conversations.length` no longer exists anywhere; (b) the new unified flow already uses defensive coalesce at every layer — backend `internal/search/search.go:89` initialises `make([]models.UnifiedResult, 0)` (returns `[]` not `null` in JSON), and frontend lines 113/115/117 use `resp.data.data \|\| {}` and `data.results \|\| []`. Old `searchConversations`/`searchMessages` API helpers still exist in `api/index.js` but no caller references them. No code commit needed. |
| FS10 | Messages tab default in search | `23d5347f` | S | – | done (covered by FS8 f89d7b82) | v1.0.3 fix reordered keys in the `results` ref from `{conversations, messages}` to `{messages, tickets}` in `frontend/src/views/search/SearchView.vue` so the messages tab rendered first by default. The fix is structurally obsolete in v2 post-FS8: there are no tabs anymore. `frontend/apps/main/src/views/search/SearchView.vue` is now a single flat list (`results` is an array, not a keyed object), and `SearchResults.vue` renders one mixed feed with no tab UI. `grep -rn "activeTab\|results\.messages\|results\.conversations\|results\.tickets"` across `views/search/` and `features/search/` returns nothing. No code commit needed. |
| FS11 | Persist search results across nav | subset of `f072968b` | S | – | done (covered by FS8 f89d7b82) | v1.0.3 source `f072968b` is a bundled commit; the FS11 half just adds sessionStorage save+restore for `searchQuery` and `searchResults` in `frontend/src/views/search/SearchView.vue`. FS8 already lands a strict superset of that behaviour in v2 `frontend/apps/main/src/views/search/SearchView.vue`: lines 90-91 restore `searchQuery` and `searchResults` from sessionStorage on mount, lines 96-98 derive the initial `searchPerformed` flag so the restored list renders without a re-fetch, and lines 121-123 write `searchQuery` / `searchResults` / `searchTotal` after every successful fetch (page 1 + load-more). FS8 also carries the defensive `Array.isArray` coerce noted in its row, which is more than the v1.0.3 fix did. The FS12 half of `f072968b` (status-change list pruning, `frontend/src/stores/conversation.js`) is independent and remains pending under FS12. No code commit needed. |
| FS12 | Auto-prune conversations from list views when status/assignment no longer matches the filter (saved views, spam, trash, assigned, unassigned) | `f072968b` | S | – | done 197cab82 | Faithful port of v1.0.3 behaviour. Hooks into v2's `mergeConversationUpdate` (deepMerge-based) instead of v1.0.3's `updateConversationProp(prop, value)`. Five prune branches: saved views (any status change), SPAM (status != 'Spam'), TRASH (status != 'Trashed'), assigned (cleared), unassigned (set). Normal inbox lists rely on the existing `conversationsList` computed for status filtering. Uses `CONVERSATION_LIST_TYPE` constants and `'status' in update` / `'assigned_user_id' in update` guards so payloads without those fields can't trigger false-positive prunes. |
| FS13 | Permanent delete from trash | subset of `e2f94c52` | M | (trash already done) | done 7e834f1d | New `DELETE /api/v1/conversations/{uuid}` (perm `conversations:update_status`) calls existing `DeleteConversation` manager method. Frontend adds a destructive "Delete permanently" bulk button visible only when `route.params.type === 'trash'`, gated behind an AlertDialog (v2 convention; v1.0.3 used `window.confirm`). After bulk delete, inline `resetConversations()` + `fetchFirstPageConversations()` per FS4 convention so deleted UUIDs don't linger in the in-memory list (`runBulkAction`'s default refresh appends, doesn't prune). Three-form pluralized i18n on the confirmation. Self-review fixed a latent v1.0.3 bug: the original handler had no Trashed-status guard, leaving the destructive endpoint reachable against any conversation by UUID — this port adds a 400 if the conversation isn't currently trashed. |
| FS14 | Search clear button | subset of `e2f94c52` | S | – | done 95f570e7 | Sidebar search icon swapped from router-link to a button that wipes `searchQuery` / `searchResults` / `searchTotal` sessionStorage keys before routing to `/search`. Same-route click attaches `?t=Date.now()` so vue-router still emits a fullPath change. SearchView gets a route watcher that clears in-memory state when sessionStorage is empty post-nav, and refocuses via a new `defineExpose`d `focus()` on SearchHeader since same-route navs don't remount the component. Adaptation delta vs v1.0.3 e2f94c52: the source also bound `/` as a global "go to search" shortcut in App.vue, but v2 has no global keydown handler (CommandBox owns Cmd+K and the `/` key inside its own input), so that half is dropped — only the icon-click clear behaviour ports. Used a raw `<button>` rather than shared-ui `<Button>` to preserve the inline icon-only look the router-link had. |
| FS15 | Agent sort | subset of `e2f94c52` | S | – | done c9592cd4 | One-line port: `.sort((a, b) => a.label.localeCompare(b.label))` appended to the `options` computed in `frontend/apps/main/src/stores/users.js`. Sorts once at store level so every consumer (assignee dropdowns on conv list / table view / ConversationListItem / sidebar / SLA form) gets alphabetical agents. No per-consumer sort needed. |
| FS16 | Dropdown height fix | subset of `e2f94c52` | S | – | done b199163c | Two-line port: `max-h-60` → `max-h-96` on the per-row Agent and Team `DropdownMenuContent` in `frontend/apps/main/src/features/conversation/list/ConversationListItem.vue` (sidebar quick-assign). Bumps from ~6-7 visible items to ~10-11. The TableView half of v1.0.3 `e2f94c52` does not apply: v2's `ConversationTableView.vue` is a stripped-down table without per-row assignee dropdowns (no DropdownMenu rendered there). The other `max-h-60` instances in v2 (`MergeDialog.vue`, the bulk-action assign dropdown in `ConversationList.vue`) are unrelated to this hunk and intentionally untouched. Final slice of the `e2f94c52` bundle (FS13 / FS14 / FS15 / FS16). |
| FS17 | Status colors in conversation list + sidebar | subset of `ebc337b3` | M | – | done 74a6efdb | New `color` TEXT column on `conversation_statuses` (migration V2_2_4 seeds Open=orange / Snoozed=purple / Resolved=green / Closed=gray to match the previously-hardcoded frontend palette), exposed through Create/Update + a new color-only endpoint `PUT /statuses/{id}/color` (color-only path skips the cannotUpdateDefault guard so the four built-ins can be recoloured without renaming). Frontend gets a single source of truth in `constants/statusColors.js` (15 Tailwind-derived presets); used by ConversationTableView (pill replacing plain status text), Conversation.vue (header pill replaces `bg-primary` with the configured colour), and admin StatusForm + a new inline `StatusColorPicker` in the admin DataTable so default statuses can be recoloured without going through the locked edit dialog. v1.0.3's drag-drop StatusView rewrite + lock-icon hunk in source `ebc337b3` was not ported — v2's existing Dialog+DataTable layout (which the maintainer prefers) was retained, with the picker grafted in. FS18 + FS19 hunks of source `ebc337b3` excluded; they ship as their own commits. Latent caching gap noted in commit body: `fetchStatuses` early-returns when statuses are already loaded, so colour edits in the admin won't propagate to live conversation tabs until reload — pre-existing pattern, out of scope. |
| FS18 | Activity log country flags | subset of `ebc337b3` | S | – | done dc05ef73 | New `country VARCHAR(2) NOT NULL DEFAULT ''` column on `activity_logs` via migration V2_2_5 (idempotent), populated from the `CF-IPCountry` request header at every log site (login/logout/availability/password/role-perms). Frontend renders the 2-letter code as an emoji flag via a small `countryFlag()` helper that offsets each letter into the U+1F1E6 regional-indicator range; output rendered into a derived `ip_display` field on each row so the existing SimpleTable just changes its key from `ip` to `ip_display`. Graceful degradation when not behind Cloudflare: empty country code yields no flag, just the plain IP. Notification-links hunk of source `ebc337b3` excluded (ships as FS19). |
| FS19 | Notification links (clickable to conversation) | subset of `ebc337b3` | S | – | done cc9fb160 | Three built-in `email_notification` template bodies in `schema.sql` get the bare `{{ .Conversation.ReferenceNumber }}` wrapped in an `<a href="{{ RootURL }}/inboxes/<list>/conversation/{{ .Conversation.UUID }}">…</a>`: SLA breach warning + SLA breached point at `/inboxes/assigned/...`, Mentioned-in-conversation points at `/inboxes/mentioned/...` (matches the existing "View Conversation" links already in those templates and the v0.10.0 / v2.2.3 precedents). Migration V2_2_6 brings existing installs up to the new seeds: each `UPDATE` is scoped to `is_builtin = true AND name = '<exact>' AND body LIKE '%<pre-link substring>%'`, so customised templates are left alone, user-created lookalikes aren't touched, and re-runs no-op (mirrors V2_2_4's defensive seed-update shape). Two notification templates the source commit deliberately didn't touch were left alone for fidelity: "Conversation assigned" already had a "View Conversation" link, and the CSAT-rating template uses `CSATLink` (different system). No Go-side changes — both `RootURL` and `Conversation.UUID` are already exposed to the notification template context (`cmd/init.go`). Final slice of the `ebc337b3` bundle (FS17 / FS18 / FS19). |
| FS20 | Merge State/Status duplicate columns in table view | `3a064d03` | S | – | done (already in v2) | v1.0.3 source commit removed a redundant "State" column (which displayed the same `conversation.status` value as a coloured pill, alongside an unstyled Status dropdown column) from `frontend/src/features/conversation/list/ConversationTableView.vue`, collapsing them into a single coloured pill dropdown. v2's `frontend/apps/main/src/features/conversation/list/ConversationTableView.vue` was rewritten as a stripped-down table and never had the duplicate column: `resizableCols` is `['contact', 'subject', 'status', 'priority', 'updated']` with one Status `<TableCell>` rendering the coloured pill via `getStatusStyle()` (FS17 landed the colour palette + pill in 74a6efdb). No separate "State" column, no `state` key in `defaultWidths`. Per-row Status dropdown affordance (the `DropdownMenu` half of the source commit) is independently absent in v2 and tracked under FS16 / FS21 — out of scope for FS20. No code commit needed. |
| FS21 | Resizable columns in table view | `f334e96e`, `a6626beb` | M | – | done (already in v2) | v2's `frontend/apps/main/src/features/conversation/list/ConversationTableView.vue` already implements every behaviour from v1.0.3's source commits, and does so using the conventions our libredesk-pr skill documents as preferred. Verified item-by-item: `<colgroup>` with reactive pixel widths (line 3-6); per-`TableHead` resize handle (line 24-29) using `group/th` parent + `group-hover/th:border-muted-foreground/25` on the handle div — that's the exact "make handles visible on hover" pattern from source commit `a6626beb`; `MIN_WIDTH = 60` clamp (line 206); double-click reset via `@dblclick="resetColumn(col)"` (line 28); checkbox column held at fixed 36px by being absent from the `resizableCols` array (line 195). Persistence uses `useStorage('conversationTableColumnWidths', ...)` (line 207-209) which is better than v1.0.3's raw `localStorage.getItem` / `setItem` calls — `useStorage` keeps multi-tab state in sync via the storage event (skill convention: "always `useStorage`, never raw `localStorage`"). Resize handlers use pointer events (`pointerdown` / `pointermove` / `pointerup`, lines 27, 220-238) instead of v1.0.3's mouse events — also a skill convention. Header labels go through `t('conversation.list.column.' + col)` rather than v1.0.3's hardcoded English. Defensive cleanup in `onUnmounted` matches v1.0.3 (lines 248-255). Default widths differ slightly because v2 has fewer columns (no separate State / Group / Agent — see FS20 + the agent/team columns dropped in the v2 rewrite) but the geometry/UX is equivalent. localStorage key name is `conversationTableColumnWidths` (camelCase, no prefix — skill convention) instead of v1.0.3's `libredesk-table-col-widths`; no migration needed since the key didn't exist in upstream. No code commit needed; spec doc updated only. |
| FS22 | Conversation list shows subject/ticket/status/priority | `bbb08b22` | M | – | done 3cb500a7 (card view) + already in v2 (table view) | Two-surface verification. **Table view** (`ConversationTableView.vue`): already covered before this port — `resizableCols = ['contact', 'subject', 'status', 'priority', 'updated']`, with `#reference_number` prefix on the subject cell, FS17 colour pill on status (via `statusColorStyle`), and a priority dot+label cell. No code change needed. **Card view** (`ConversationListItem.vue`): had the subject line but was missing reference number, status pill, and priority indicator. Ported in 3cb500a7: status pill + priority dot added to the right end of the name+inbox row using the same `statusColorStyle` source as table view (so admin colour edits propagate uniformly), reference number prefixes the subject line in the same `<p>` so a `#ref`-only row (subject not yet set) still surfaces the ref. Priority rendered as a coloured dot with `title` tooltip rather than a labelled pill — card rows are already dense (name, inbox, status, time on top row; subject below; preview below; SLA badges and assignee dropdowns conditionally below that), and a pill would crowd. `priorityDotClass` duplicates the table-view switch; two callers don't justify extracting a shared helper. v1.0.3's hardcoded status-colour palette + freshdesk theme overrides not ported — FS17 supersedes the colour palette half (admin-configurable per-status colours), and the freshdesk theme is fork-only. |
| FS23 | Sidebar filter panel (Freshdesk-style slide-out with named sections) | subset of `13edaf3e` (bundle also covers T3v voice transcription + threading fixes) | M | FS2 | done 8635964b | New `ConversationFilterPanel.vue` ported under `frontend/apps/main/src/features/conversation/list/`. Sections: Contact Email, Status (hidden on spam/trash), Agents include/exclude, Teams include/exclude, Priority, Tags, Created, Last activity, Closed at, Resolved at, SLA deadline. Coexists with FS1's pill bar — pill bar stays for ad-hoc/power-user filters, panel covers the common Freshdesk-named-field path. State is store-derived (reads/writes `conversations.adHocFilters` + `conversations.status` via existing setters), never owns its own copy, so per-view persistence from FS3 + back-nav refresh from FS4 carry panel state across view switches and tab reloads with zero extra wiring. Operators normalized to v2 conventions: tags `contains` (space form, not v1.0.3's `_`), other multi-selects `in`/`not_in`, dates `relative_date` with the preset strings the SQL builder already understands. Click-outside uses `pointerdown` per skill notes. Hidden `SheetTitle` for reka-ui screen-reader compliance; v1.0.3's transparent-overlay CSS hack dropped (targeted a `data-radix-overlay` attribute reka-ui never emits). 20 new i18n keys under `conversation.filterPanel.*`. ConversationList.vue gains a `Filters` trigger button with count badge in the right-side controls + the panel mount; pill bar untouched. T3v + FS24 portions of the source bundle deferred to their own units. |
| FS24 | Search & threading fixes from `13edaf3e` bundle | subset of `13edaf3e` (other portions are T3v voice + FS23 sidebar) | S | – | done a4dbe3d9 | Residual fixes from the bundle that also fed T3v / FS23. File-by-file disposition: (1) `internal/search/queries.sql` — exact ticket-number matches now sort first via `CASE WHEN reference_number::text = $1 THEN 0 ELSE 1 END` in the outer `ORDER BY` (was missing in v2). Ported. (2) `internal/conversation/queries.sql` + `conversation.go` + `message.go` subject+contact threading fallback — already in v2 (verified `find-conversation-by-subject-contact` query, `FindConversationBySubjectContact` stmt, and `message.go:1551` lookup all present). No action. (3) `internal/conversation/conversation.go` `Contact` map in mention notification template render — was missing in v2 (`NotifyMention` had Recipient/MentionedBy/Author but no Contact, leaving `{{ .Contact.FullName }}` etc. unresolvable in custom mention templates). Ported. (4) `frontend/apps/main/src/features/conversation/ReplyBox.vue` mention-only private notes — was missing in v2 (`hasContent` and `hasDraftContent` gated only on text+attachments; TipTap mention nodes don't always count toward `textContent`, so `@john` alone silently no-op'd). Added `mentions.value.length > 0` to both gates; mentions only attach when `isPrivate`, so non-private send behaviour is unchanged. Ported. (5) `frontend/apps/main/src/features/conversation/message/MessageList.vue` scroll-to-last-message — was using `scrollToBottom`. Added `scrollToLastMessage` helper that scrolls the last *non-activity* message to ~12px from the top with falls-back to scrollToBottom; the conversation watcher now calls this for normal opens (mentioned-conversation `?scrollTo=` path unchanged). Ported. (6) `frontend/apps/main/src/features/conversation/list/ConversationList.vue` "1 Filters" Trash/Spam fix — covered by FS23 (`8635964b`); the FS23 panel's `activeFilterCount` already excludes status-as-filter for spam/trash views via `NO_STATUS_VIEWS` guard. No action. (7) `frontend/apps/main/src/features/conversation/list/ConversationTableView.vue` chevron `truncate max-w-full` + `priority/status` width tweaks — v1.0.3 hunk targets old markup that v2 doesn't have; v2's table view (post-FS17 / FS25) renders status as a coloured pill button and priority as a dot+label dropdown with different layout, and `whitespace-nowrap` is the deliberate convention here. Default widths in v2 are already wider (`status: 100`, `priority: 100`). No action. (8) `cmd/media.go` audio-inline + `internal/conversation/{queries.sql,message.go,transcribe.go}` audio-transcribe + `cmd/main.go` worker init + `frontend/.../MessageAttachmentPreview.vue` audio player + `frontend/.../AISettings.vue` whisper config + `internal/setting/models/models.go` AISettings — all T3v territory, out of FS24 scope. |
| FS25 | Inline status + priority dropdowns on conversation list rows | subset of `634f538c` (other portions are the table view basics already done in 5.0 `20eb7d1b`/`6abfd4f8`) | S | T1a (done) | done `2bdedf71` | Sibling to T1a. T1a ported the per-row **agent + team** chip dropdowns from `fe5fefb7`; this ports the per-row **status + priority** dropdowns from `634f538c`'s table-view commit. v2 baseline showed both as static pills (no click-to-change); v1.0.3 made them interactive. Implementation: wrapped status pill + priority dot in `DropdownMenu` triggers, items pull from `conversationStore.statusOptionsNoSnooze` and `conversationStore.priorityOptions`, on click call `api.updateConversationStatus` / `api.updateConversationPriority` then `conversationStore.updateConversationField` (the SS6 helper, not direct prop mutation). Ported symmetrically into both `ConversationTableView.vue` and `ConversationListItem.vue`. Two improvements over the v1.0.3 source: each affordance is permission-gated (`conversations:update_status` / `conversations:update_priority`) — falls back to a static pill/dot when the agent lacks the permission, instead of surfacing backend 403s on click. And the priority menu items show a coloured dot next to the label for faster visual scan. Snoozed deliberately omitted from the inline status menu (matches `statusOptionsNoSnooze`) — it needs the duration picker the conversation detail toolbar provides, not appropriate for a one-click list affordance. |

### 5.5 Macros & per-inbox config

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| MP1 | Per-inbox email signatures with dynamic placeholders | `da939456`, `735feff8`, signature-spacing portion of `dedaf0e8` | M | – | done a59d1d80 | Backend: `Signature` field on inbox Config, `GET /api/v1/inboxes/{id}/signature?conversation_uuid=` resolves placeholders against caller + contact. Frontend: textarea + 6 placeholder quick-insert buttons in inbox admin; ReplyBox auto-inserts on mount + conversation switch when editor empty + reply mode. Signature-spacing portion of dedaf0e8 not specifically ported — re-open if formatting issues surface. T2j (EC19 private note signature stripping) now unblocked. |
| MP2 | (absorbed) | – | – | – | n/a | Per-inbox From switcher absorbed into EC14; macro toolbar absorbed into EC15. |
| MP3 | (absorbed) | – | – | – | n/a | Per-inbox AI settings moved to T3h (Tier 3 dependency). |
| MP4 | Email aliases per inbox | subset of `cf97cb06`, `9f23b9f3`, alias portion of `dedaf0e8` | M | – | done 4c386b0d + fix fd224363 | Absorbed into EC14 (T2h). Aliases managed in inbox admin via EmailTagInput chip UI; persisted in `inboxes.config.aliases`; surfaced as the From-switcher option list in the reply box. fd224363 hardens the absorbed flow: dedup key includes the alias so an agent can re-send the same body under a different alias without a 409. |
| MP5 | Macro attachments (upload files to macros, auto-clone into replies) | `2a898da6` | M | – | done f80a3bf6 | Spec note "DB schema for macro_attachments" was misleading: no schema change needed. The `media` table is already polymorphic (`model_type` + `model_id`), so a new `ModelMacros = "macros"` constant is enough. Attachments live in the same table as message media; an additional pseudo-field `Attachments []Media` is populated on the `Macro` model by the handler (`GetByModel` doesn't fill `URL`, so a `populateMacroAttachments` helper does it inline — single-row `Get` already does). New `macroRequest` envelope wraps `models.Macro` with `attachment_ids []int`; Create attaches them post-create, Update detaches all then re-attaches the submitted set (so removing a file in the admin form removes it on save), Delete now also calls `DeleteModelMedia`. New `POST /api/v1/macros/{id}/clone-attachments` (auth gate, matching `handleApplyMacro`) calls `media.DuplicateForModel('macros', id)` — re-reads each blob, generates a new UUID, copies the thumbnail best-effort, and inserts an unlinked-message media row (`model_type='messages'`, `model_id=NULL`) that the existing send-message pipeline will pick up. Storage write is rolled back on DB-insert failure so we don't leak orphans. Cloning rather than referencing means the agent can later delete the macro (or its files) without breaking already-sent messages. Frontend: `MacroForm.vue` adds file picker via existing `AttachmentsPreview` + `useFileUpload({linkedModel: 'macros'})`; ReplyBox + CreateConversation macro-selection watchers go async — when the chosen macro has attachments, they call the new clone endpoint and append the result via `setMediaFiles([...mediaFiles.value, ...cloned])` (mediaFiles is readonly from the composable, hence re-set rather than push). i18n reuses `globals.terms.file` (plural) and `globals.messages.attachFile` — no new keys. |
| MP6 | Per-user macro usage tracking + most-recently-used sort order | `a894adc9` | S | – | done c38f61bf | New table `macro_user_usage(user_id, macro_id, last_used_at, use_count)` with PK `(user_id, macro_id)` and an index on `(user_id, last_used_at DESC)`. Schema added to `schema.sql`; existing installs picked up via new migration `V2_2_7` (idempotent `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`). New SQL queries `get-all-for-user` (LEFT JOIN with the user-specific row, ORDER BY `last_used_at DESC NULLS LAST, name ASC`) and `mark-used` (upsert that bumps timestamp + count). New manager methods `GetAllForUser(userID)` and `MarkUsed(userID, macroID)`. New `LastUsedAt *time.Time` field on the Macro model (nil for `Get` / `GetAll`, populated only by `GetAllForUser`). `handleGetMacros` switches to `GetAllForUser(auth user.ID)`. `handleApplyMacro` calls `MarkUsed(user.ID, macro.ID)` after successful apply (failure logged, non-fatal). Frontend store gets `markUsedLocal(macroId)` for optimistic re-sort using the same ordering predicate the backend applies; `overrides/.../Conversation.vue` (executePendingSend) and `CreateConversation.vue` call it after the apply API succeeds so the picker reorders without a refetch. Existing `GetAll()` (used by RAG sync) is left alone. |

### 5.6 Inbox / IMAP plumbing

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| IP1 | Comma-separated IMAP folders (multi-folder polling per inbox) | `0794f066` | S | – | done `be803a12` | Splits `cfg.Mailbox` on `,`, polls each. |
| IP2 | Sanitise incoming email HTML | `9ff809ac` | S | SS4 | done `3b7ef284` | The "use shared sanitiser when SS4 lands" hint in the original spec was wrong: SS4's `SanitizeHTML` wraps `bluemonday.UGCPolicy()`, which is built for forum-style user-generated content and strips inline styles, classes, and most table attributes. Running it on incoming email would mangle legitimate marketing/transactional templates. v1.0.3's IP2 was a display-quality whitespace pass, not an XSS boundary; its commit message ("Removes excessive whitespace, empty divs, and multiple <br> tags … for cleaner display") confirms the intent. Faithfully ported the original five-regex pass as a sibling helper `stringutil.SanitizeEmailHTML` in the same `sanitize.go` (Outlook elementToProof divs, empty divs, whitespace-only divs, runs of `<br>`, runs of `\n`, then trim). Applied in `ProcessIncomingMessage` just before `InsertMessage`, on the local `msg.Content` (v2's flat IncomingMessage delta), gated on `ContentType == "html"` (regexes are no-ops on plaintext; gate is cheaper and self-documenting). `InsertMessage` regenerates `TextContent` from the sanitised body via the existing `HTML2Text` call, so the search index sees the cleaned content too. |
| IP3 | Prevent duplicate outgoing emails (set sending status before SMTP call) | `b0e733af` | S | – | done `9f8ae403` | The brief said v1.0.6's `d80c7791` (`sending` enum value) had already landed in v2 — it had not. This commit ports both halves: the code change (`UpdateMessageStatus(uuid, MessageStatusSending)` immediately before `inb.Send` in `sendOutgoingMessage`) and the missing schema infrastructure. Schema: adds `'sending'` to `message_status` enum in `schema.sql` for fresh installs; new migration `V2_2_8` (idempotent `ALTER TYPE ... ADD VALUE IF NOT EXISTS`) for upgrade path; registered in `cmd/upgrade.go` migList. New `MessageStatusSending` constant in `internal/conversation/models/models.go`. Closes both holes the in-memory `outgoingProcessingMessages` exclusion map leaves: process restarts mid-send, and the brief in-flight window when SMTP is slow. The pending-scan query in `queries.sql:598` already filters `WHERE m.status = 'pending'` so a row marked `sending` is invisible to it — no query change needed. Status transitions: `pending → sending → sent` on success, `pending → sending → failed` on send error (existing `handleError` path). Websocket clients see one extra status update per send, harmless. |
| IP4 | Increase HTTP read/write timeouts to 30s | `eda75f6a` | S | – | done `39f35988` | Brief was wrong on two counts. (1) Not SMTP — this is the fasthttp HTTP-server timeout in `cmd/main.go` (`ReadTimeout` / `WriteTimeout` read from `app.server.read_timeout` / `write_timeout`), which governs how long the server will wait for an inbound request body or response flush before killing the connection. The original v1.0.3 fix was for agents on high-latency uplinks (China) failing to post replies once quoted-thread bodies got large — fasthttp closed the socket mid-upload at 5s. (2) v1.0.3 also bumped `read_buffer_size` from default to 16KB; v2.1.1's `config.sample.toml` already sets it to 65536 (64KB), which exceeds the v1.0.3 target, so the buffer half is a no-op (porting it would be a regression). Code change is `config.sample.toml` only — `5s → 30s` for both `read_timeout` and `write_timeout`, with an inline comment explaining the latency reasoning so future maintainers don't tighten it back. Existing installs keep their on-disk values; only fresh installs from the sample see the new defaults. No Go code paths affected (`cmd/main.go:302-306` already reads these via `ko.MustDuration`). |
| IP5 | Resolve agent/team names on WebSocket assignment updates | `c4c048d3` | S | – | done `3f1338eb` | Brief was wrong: this is a frontend fix, not a backend change. Backend `UpdateAssignee` (`internal/conversation/conversation.go:839`) already broadcasts only the ID — that's the correct, narrow payload. The gap is on the receiving side: `mergeConversationUpdate` in `frontend/apps/main/src/stores/conversation.js` deep-merges the ID but `assigned_user_name` / `assigned_team_name` stay stale, so other agents see the *previous* assignee in the list/header until refresh. Ported v1.0.3's frontend resolver: after the deep-merge, look up the new ID in the already-loaded `useUsersStore()` / `useTeamStore()` `.options` arrays and write the resolved label into the target. Adapted v1.0.3's `{prop, value}` payload to v2's flat update-object shape — checks `'assigned_user_id' in update` and `'assigned_team_id' in update` independently so a future broadcast carrying both IDs would still work. Falls through silently when the store hasn't been hydrated (preserves existing name); explicitly clears name on `null` ID (covers `RemoveConversationAssignee` at line 1587). Applied to both `conversation.data` (open conversation header) and `existing` (list row). Backend-resolution alternative rejected — adds a DB hit per assignment to refetch a name the frontend already has. |
| IP6 | Fix contact upsert to handle domain-like names | `fa21a17a` | S | – | done `15b9c37b` | v2 had restructured v1.0.3's single `insert-contact` into two paths: `insert-contact-with-external-id` (ON CONFLICT external_user_id, was overwriting name unconditionally) and `insert-contact-without-external-id` (ON CONFLICT email, race-condition fallback since Go pre-checks via `GetContactByEmail`). Applied v1.0.3's name-protection logic to **both** paths: only overwrite stored first/last name when EXCLUDED.first_name is non-empty AND stored first_name is empty/null or contains a dot (domain-like marker). EXCLUDED.first_name gates both first_name and last_name updates, matching v1.0.3's bias. v2's smarter `getContactName` already derives readable names from email local parts ("sharyn@x" -> "Sharyn") which mitigates the new-contact side, but the upsert protection still defends (a) existing contacts whose stored name was set by an older build to a domain-like value, and (b) other callers (SDK, JWT widget sync) passing garbage first_name. No Go changes needed — prepared statements unchanged. |
| IP7 | Spam-folder rescue for known senders + IMAP MOVE-back to INBOX | `a427efc2` (deploy auto-commit bundling all of 2026-05-04's work — extract via `git log --grep=spam` / file paths `internal/inbox/imap_unspam.go`, `internal/conversation/{conversation,message,queries.sql}`) | S | IP1, 5.0 Spam&Trash, T3y (soft — shares IMAP-write template with `imap_delete.go`) | done `711a7f40` | Two-part feature shipped as one commit. **(a)** Gates the existing `isSpamMailbox → MarkAsSpam` call in `processIncomingMessage` on the new `ContactHasPriorAgentReply` helper (backed by `contact-has-prior-agent-reply` query). On lookup error we default to the safer mark-as-spam branch so a transient DB hiccup can't route untrusted mail to Open. **(b)** New `internal/inbox/imap_unspam.go` issues IMAP MOVE from the configured spam folder back to INBOX. Triggers from auto-rescue path (via new `IMAPUnspamFunc` callback on conv Manager, wired in `cmd/main.go` as `conversation.IMAPUnspamFunc = inbox.UnspamIMAPMessage` to keep the dependency one-way) and from manual `handleMarkAsNotSpam` (uses new `get-latest-incoming-source-id` query). MOVE auto-falls-back to COPY+STORE+EXPUNGE via the go-imap client. v2 adaptation deltas: v2's `IncomingMessage` is flat (no nested `.Message`) so the rescue references `msg.SourceID` post-insert and `in.Contact.ID` directly; v2's `MarkAsSpam`/`MarkAsNotSpam` already take a `umodels.User actor` (UX22-style — the v1.0.3 source pre-dated UX22) so the gate slots in around the existing `systemUser` lookup with no signature churn; v2's `GetDBRecord` already decrypts the inbox config so `imap_unspam.go` reads plaintext passwords directly. Small enhancement on top of v1.0.3: rescued spam messages now also fire the `notifyParticipants` notification (v1.0.3 was suppressing notifications for *all* spam-folder messages including rescued ones — a latent gap, since the conversation stays Open). T3y's `imap_delete.go` doesn't exist yet in either source, so we introduced the IMAP-write pattern fresh; when T3y lands a small refactor can extract a shared helper. OAuth IMAP unspam still falls back to a stored password when present (or returns an explicit error otherwise) — true XOAUTH2 here would require relocating the OAuth refresh helpers from `channel/email` into the `inbox` package, deferred until T3y forces the issue. |

### 5.7 Security & sanitisation

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| SS1 | Validate inbox ID override on message send | `a444006b` | S | – | done (already in v2) | The v1.0.3 abuse vector was a `req.InboxID` field on the per-message reply API (`POST .../messages`) introduced by our own From-switcher work in `97533510`; an authenticated agent could override the conversation's inbox to send through an arbitrary or disabled inbox. v2's port of the From switcher (`4c386b0d` — EC14) deliberately routes the override via `meta.from` (validated against the inbox's primary From + configured aliases by `validateInboxFromOverride` at `cmd/messages.go:428-434`) and never accepts an `inbox_id` field on `messageReq` — `QueueReply` is hardcoded to `conv.InboxID` (`cmd/messages.go:445`). v2 also already validates that conversation inbox exists and is enabled at `cmd/messages.go:344-352` before queueing. The new-conversation path (`cmd/conversation.go`) does take `inbox_id` but `validateCreateConversationRequest` lines 951-961 already check exists/enabled/email-channel. No code change needed — the architectural decision in `4c386b0d` removed the abuse vector at the API surface. |
| SS2 | SSRF protection for external URL fetching | `f3e7bd9e` | M | – | done `8c8364f3` | The v1.0.3 patch protected RAG webpage syncing and external search — neither is ported to v2 (RAG is fork-only). The real SSRF surface in v2 is OIDC: `internal/auth/auth.go` accepts admin-supplied `ProviderURL` and hands it to `oidc.NewProvider`, which does an HTTP GET on `<url>/.well-known/openid-configuration`. An admin (or attacker who compromised one) could submit `http://169.254.169.254/...` to probe cloud metadata, `http://localhost:5432/` for Postgres, etc. Reused the existing `github.com/abhinavxd/ssrfguard` dep (already used by webhook manager) instead of porting v1.0.3's hand-rolled `internal/urlutil/ssrf.go` — ssrfguard is a `net.Dialer.Control` callback that fires post-DNS, pre-TCP-handshake, which closes the DNS-rebinding TOCTOU gap the v1.0.3 implementation had. Coverage strictly broader than v1.0.3 (adds CGNAT, TEST-NETs, multicast, IPv6 unique-local, etc). Custom `*http.Client` lives on the Auth struct, used by `New`, `Reload`, `TestProvider` (via `oidc.ClientContext`), and `ExchangeOIDCToken` (via `oauth2.HTTPClient` ctx value — the IdP could publish a private-IP token endpoint in its discovery doc, belt-and-braces). New `[oidc] allowed_hosts` config option lists CIDR prefixes permitted to bypass for self-hosted IdPs on private subnets; empty by default. `Reload` rebuilds the client only when the allow list actually changes. Latent bug noticed: `reloadAuth` in `cmd/init.go` was already dropping `SecureCookies` and `SessionLifetime` on reload (passed empty Config) — out of scope, left alone, only added `AllowedHosts` to keep parity with what `Reload` now reads. |
| SS3 | Prevent internal error details leaking to clients | `05823ba0` | S | – | done (folded into T3q) | The fix is a one-line change in `cmd/ecommerce.go` (`handleTestEcommerceConnection` — log full error server-side, return generic "Connection failed. Check your settings and try again." to the client). That file does not exist in v2 yet; ecommerce hasn't been ported. Per the spec's source-of-truth scope rule ("port commit reflects the feature's current working state on v1.0.3, not its initial buggy implementation"), T3q (`80f808b0` — Ecommerce API handlers) must port the post-SS3 version of `handleTestEcommerceConnection`. Verified no other v2 endpoints leak internal error details from this commit's pattern (other `err.Error()` returns in `cmd/` are either i18n-wrapped parse errors with err in the data field, or upstream's pre-existing pattern that SS3 did not touch). No standalone code commit required. |
| SS4 | Sanitise HTML in contact + private notes | `0ba4240d` | M | – | done `a41b374b` | Faithful port. New `internal/stringutil/SanitizeHTML` wraps `bluemonday.UGCPolicy()` (matches v1.0.3 lib + version v1.0.27). Applied at the two sinks v1.0.3 hit: `handleCreateContactNote` (cmd/contacts.go) and `handleUpdatePrivateNote` (cmd/messages.go). Did NOT extend to private-note creation (`SendPrivateNote`) — v1.0.3 didn't, and TipTap sanitises on the way in. IP2 can now reuse `stringutil.SanitizeHTML` directly for incoming email HTML. `bluemonday` lands as a direct dep (v2's go.mod is properly tidied; v1.0.3 had it as indirect because that go.mod was stale). |
| SS5 | Clear search timeout on EmailTagInput unmount | `e67f4f0c` | S | – | done (already in v2) | The `onUnmounted` cleanup that clears the dangling `searchTimeout` on debounced contact search is already present in v2's `frontend/apps/main/src/components/EmailTagInput.vue` (lines 85, 102, 104-106). Carried over when the chip-input was ported to the monorepo (likely as part of EC8 `765d8490` or EC14 `4c386b0d`). Confirmed `searchTimeout` only appears in this one file across the v2 frontend. No code change needed. |
| SS6 | Use store method instead of direct prop mutation (image work) | `4f88724b` | S | – | done `6196a8bb` | Partial port. v1.0.3 mutated `props.conversation.*` from two places: `ConversationListItem.vue` (assign/unassign agent + team) and `ConversationTableView.vue` (assign/unassign agent + team + priority + status). v2's `ConversationTableView.vue` has been redesigned into a click-through display table — the inline assign/priority/status dropdowns aren't there, so that half is N/A by redesign. The `ConversationListItem.vue` half is still present in v2 and was ported faithfully: new `updateConversationField(uuid, field, value)` helper on the conversation store + replaced the four `props.conversation.assigned_*_name = ...` mutations with `conversationStore.updateConversationField(...)` calls. Note: section 5.0 row references the fix as part of an "image bundle" hash `ff8b2223` — that hash does not exist on the v1.0.3 fork; SS6 is a standalone fix tied only to `4f88724b`. |
| SS7 | Session validation log enrichment | `c42b470a` (middlewares.go portion) | S | – | done `667818d8` | `cmd/middlewares.go` was logging `"error validating session" error=null` whenever the session call returned no session — opaque about whether the cookie was missing, the session store was unreachable, or the record was empty/expired. Replace the single-field log with a `reason` enum (`missing_cookie` / `session_store_error` / `empty_session` / `unknown`) plus `path`, `method`, `cookie_present`, `cookie_len`, `session_user_id`. Pure instrumentation, no behaviour change. Helped diagnose the "Test Connection works but Lookup buttons fail" red-herring during the Maho v2 debug session — narrowed the real cause (frontend export bug) by ruling out auth. v2 file: `cmd/middlewares.go` (same path); same code shape so port should drop in. |

### 5.8 Recent activities feed

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| RA1 | Recent activities feed under Reports + auto-purge | `52cdd110` | L | – | done `0d348c70` | Backend: new `/api/v1/reports/activities` endpoint (reports:manage), capped at 50/page. `get-recent-activities` joins `conversation_messages` (filtered to `type IN ('activity', 'outgoing')`) → `conversations` → `users` with COUNT(*) OVER() for paginated total in one round trip. `purge-old-activities` deletes only `type='activity'` rows older than the retention window, never agent replies or customer messages. `TrashSettingsFunc` tuple extended from 3 to 4 ints; `RunTrashManager` cycle gains the activity-purge pass. New `trash.activity_purge_days` setting (default 7, 0 disables) seeded in `schema.sql` for fresh installs + idempotent migration `v2.2.9` for upgrades. Brief mentioned a `trash_manager.go` comment about deferred activity-purge plumbing — no such comment existed in v2; the v1.0.3 spam/trash port (`d71303f6`) just hadn't included activity purging at all. RA1 fills that gap. Frontend: `RecentActivitiesView` uses shared-ui Avatar/AvatarFallback for actor initials (replacing v1.0.3's hand-rolled HSL avatar — better fidelity to v2's contact list pattern), Spinner, Button, and shared `getRelativeTime`/`formatFullTimestamp` helpers (no `formatMessageTimestamp` import needed). Toast on fetch error via `useEmitter`. Reports nav entry behind `reports:manage`; Sidebar iconMap gains the lucide `Activity` icon. Trash admin form gains the new field; en.json adds 5 keys, alphabetically sorted. |

### 5.9 UX polish / small fixes

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| UX1 | Suppress notification when self-assigning a conversation | `9ed7390f` | S | – | done (already in v2) | Upstream `8fd05667a` (2026-03-25) — same guard `assigneeID != actor.ID` ships in v2.1.1. |
| UX2 | HEIC/HEIF auto-convert to JPEG on upload | `41ffdf2b` | M | – | done `d1981141` | Server-side image conversion before storage. Detects HEIC/HEIF by content type (`image/heic`, `image/heif`, +`-sequence` variants) or filename extension in `UploadAndInsert`, then shells out to `heif-convert -q 90` (from `libheif-tools`, added to the Dockerfile) on a temp file. Replaces `content`/`srcFilename`/`contentType`/`fileSize` before the existing `m.Upload` runs, so downstream content-type detection sees JPEG bytes. Conversion failure logs a warn and falls through to storing the original — preferable to rejecting the upload since the worst case is just a file the agent can't preview, vs losing it entirely. v2 file structure matches v1.0.3 exactly so the diff applied 1:1 (2 files, 74 insertions, 1 deletion). Hosts running outside Docker need `libheif-tools` (or distro equivalent) installed; the no-op fallback keeps existing installs working. |
| UX3 | Standalone Close button in conversation header | `4c2b1d3a` | S | – | done `8f4067af` | Quick-resolve next to status menu. Faithful port to the active `overrides/features/conversation/Conversation.vue` (the file `executePendingSend` runs from). Button sits inside the existing right-side flex container, before the status DropdownMenu — wrapper switched from `flex items-center` to `flex items-center gap-1` so the button has natural breathing room from the pill. Visibility gate `status !== 'Closed' && status !== 'Trashed'` matches v1.0.3 verbatim; Spam stays clickable on purpose (closing a Spam conversation is a legitimate path). Reuses existing imports — `Button` from shared-ui, `CONVERSATION_DEFAULT_STATUSES.CLOSED` for the click target, `t('globals.messages.close')` for the label. No new keys, no new imports. |
| UX4 | Pending updates pill (banner showing config drift, etc.) | subset of `e918b386` | M | – | done `d2ff7456` | Sliced from the multi-feature `e918b386` (the shipping/SKU portions stay with T3u). v2 adaptation: v1.0.3 hooked the buffer into a now-absent `addUpdatedConversation`, but v2 routes WebSocket new-message events through `refreshConversationList` (followed by deep-merge in `mergeConversationUpdate` / `updateConversationMessage`). Moved the buffer point there: `refreshConversationList` now takes a `conversationUUID` arg and only increments `conversations.pendingUpdates` when the row isn't already in the list; `addNewConversation` does the same. New `applyPendingUpdates()` resets the counter and refetches; `resetConversations` zeroes it on view switch. Pill rendered between filter header and list body in `ConversationList.vue`, gated on `pendingUpdates > 0`, plain `<button>` (not shared `Button`) because the styling is a bespoke rounded pill matching v1.0.3 aesthetics. New `conversation.list.pendingUpdates` plural key (3-form, alphabetical insert). Dead `debouncedFetchFirstPage` helper removed since nothing calls it any more (`debouncedFetchParticipants` stays). v1.0.3 commit subject also mentions multi-status checkbox filter, but the actual diff didn't contain it — that came in a different commit and isn't in scope here. |
| UX5 | Followers system on conversations | subset of `9513bd98` | M | – | done ec30fee1 | Reuses existing `conversation_participants` table (no schema change beyond migration `v2.2.10` seeding the new "Added as follower" email template). Backend: `addConversationParticipant` exported as `AddConversationParticipant`; new `RemoveConversationParticipant` + `delete-conversation-participant` query. `get-conversation-participants` picks up a `users.type='agent'` filter so the followers picker / `notifyParticipants` recipient set are agent-only (System user is still type='agent', so the frontend strips that by name). `InsertMessage` flips the auto-add-sender path to contacts-only — agents now follow explicitly via the new endpoints rather than getting silently subscribed by every reply they make. New `NotifyFollowerAdded` on the conversation Manager mirrors `NotifyMention` shape; dispatches via the existing notif dispatcher (in-app + WS + email) when an agent adds another agent as a watcher (self-add skipped). Four routes: `POST /follow` + `DELETE /follow` (auth, self-toggle) and `POST/DELETE /followers[/{user_id}]` (perm `conversations:read`, manage another agent). Frontend: SelectTag picker under the sidebar Actions accordion uses a computed get/set that diffs added/removed IDs and dispatches one API call at a time, with an in-flight guard so the optimistic SelectTag emit doesn't race the server response (server returns the refreshed list as the truth). One-click Follow / Unfollow in the conversation-header More-actions menu (eye / eye-off icons) for the self-path; uses optimistic toggle with revert-on-error. Three new i18n keys (`conversation.follow`, `conversation.unfollow`, `placeholders.selectFollowers`) plus `notification.addedAsFollower` with `{author}` / `{referenceNumber}` placeholders, all alphabetically sorted. Adaptation deltas vs v1.0.3 9513bd98: (a) v2's existing `notifyParticipants` (TmplNewReply, landed pre-RA1) already covers watcher-gets-notified-on-reply — no separate dispatch on message insert needed, only the contacts-only auto-follow tweak; (b) v1.0.3's hardcoded "Invalid user_id" handler strings replaced with `errors.parsingRequest` so the message localises; (c) header toggle wired into the override `Conversation.vue` (post-EC3 active file) rather than v1.0.3's pre-override path; (d) followers picker reuses `@shared-ui` SelectTag instead of v1.0.3's components/ui path; agent options come from `useUsersStore().options` which App.vue already populates globally. UX18 hunks of `9513bd98` (contact change + quick-create + subject inline edit + team-inbox-show-all + email-recipients reshape + activity-log layout + search expansion + per-inbox AI settings + sender-name resolution) deliberately left for the UX18 unit. |
| UX6 | Custom statuses + message bubble colour overrides | subset of `ab5044aa` | M | – | done 88ab22ae (covered by FS17 74a6efdb for status colour pills + custom-status creation flow) | After FS17 shipped status colour pills + the custom-status Dialog (name/category/colour), the residual UX6 slice is just three things: (1) subtle bubble background fills so agent (outgoing) and customer (incoming) messages are visually distinct beyond left/right alignment — done via two new tokens (`--bubble-agent`, `--bubble-customer`) registered in `tailwind.config.cjs` as `bg-bubble-agent` / `bg-bubble-customer`, applied conditionally in MessageBubble's `bubbleClasses` (gated to `!private && !forward` so `bg-private` still wins for those). v1.0.3's hardcoded `#F6F7F9` / `#FFF` would have inverted in dark mode; tokens carry through the `.dark` ruleset cleanly. (2) Status name max length 25 → 50 in both backend (`status.go`) and frontend zod schema (`formSchema.js`) — bulk-imported canned status labels and descriptive names ("Waiting on supplier reply") need the headroom; 50 still fits the inline pill. (3) Send-and-set-as dropdown widened from fixed `w-56` to `w-auto min-w-[14rem] max-w-[24rem]` with `text-xs whitespace-nowrap py-1.5` items, so long custom names + translated labels don't truncate. **Skipped:** custom-status creation flow (already in v2 — Dialog + name/category/colour form); `show_on_send` toggle (superseded by v2's category-based filter in `availableSendStatuses` — every status has `category` ∈ {open/waiting/resolved}, dropdown shows open+resolved only, which is cleaner than a per-status flag); drag-drop reorder + `sort_order` column (conflicts with v2's DataTable admin pattern the maintainer prefers, and `sort_order` isn't displayed anywhere except that admin view). Email threading hunks of `ab5044aa` are tracked under UX7. |
| UX7 | Email threading improvements | subset of `ab5044aa` | M | ER3 | done eebdfbe5 | Outbound replies now include the last 3 prior non-private messages as a Gmail-style quoted thread (`<div class="gmail_quote">` + `<blockquote>` per message). New `get-previous-email-messages` query + `getPreviousEmailMessages` manager helper + `models.QuotedMessage` DB-only struct. Append happens in `sendOutgoingMessage` before `RenderMessageInTemplate` so the agent's signature/template wraps the full body. Gated to email channel and `!isForward` (forwards already get a "Forwarded message" header from a different path). Meta unmarshal hoisted above template render so `isForward` is known in time; the hoist is otherwise inert (Meta isn't read by render or attachment code). v1.0.3 9b89add1's later `<!-- thread -->` skip-marker is intentionally not ported — v2's reply path doesn't emit the marker, and ER1's storage-side strip already guards against any future client-built thread doubling with the server append. The quoted thread is only added to the in-memory outbound copy; the persisted message row is unchanged so agents see their pristine reply in the message list while recipients see the threaded copy. |
| UX8 | Session timeout polish + Ctrl+K guard | subset of `dedaf0e8` | M | – | done 073fac5d | Two unrelated polish fixes. (1) Session lifetime default flipped from 9h fixed to 96h sliding (`defaultSessionLifetime` constant + `st.SetTTL(lifetime, true)` in `internal/auth/auth.go`). 96h survives Friday-to-Monday so agents aren't logged out over the weekend; sliding so an active session keeps extending. The `app.server.session_lifetime` config knob already exists in v2 — admins can still override, sliding applies either way. `config.sample.toml` updated to match. (2) `CommandBox.vue` Ctrl+K guard: `useMagicKeys` was opening the macro picker on synthetic events fired by Grammarly and during IME composition. Added `ignoreNextShortcut` flag set in `onEventFired` when `e.isComposing \|\| !e.isTrusted`, gated the watch handler on it. Other hunks of `dedaf0e8` are out of scope here: signature-spacing rolled into MP1, email-aliases ref rolled into MP4/EC14, table-layout:fixed removal + default sort change belong to other UX units. |
| UX9 | Drag-drop all file types + attachment previews + private note fixes | subset of `fc5d8f1a` | M | – | done c663d292 | Verify-first paid off. The v2 image bundle (`ff8b2223`) had already shipped: TextEditor's drop split (image vs non-image, emit `filesDropped` for the latter); image-attachment lightbox; PDF iframe preview overlay in `FileAttachmentPreview.vue`; MergeDialog `min-w-0`/`shrink-0` subject truncation. Residual gap was tiny: (a) nobody upstream of TextEditor was listening to the `filesDropped` event, so non-image drops silently fell on the floor — wire it through ReplyBoxContent (override + base) → ReplyBox → `uploadFiles` from useFileUpload, which routes the File[] through the same upload path as the paperclip button; (b) Send-button copy stayed "Send" / "Send and set as ..." even when the agent was composing a private note — pass `messageType` from ReplyBoxContent down into ReplyBoxMenuBar, button label now flips to "Add note" / "Add note and set as {status}" when `messageType === 'private_note'`. Two new i18n keys (`replyBox.addNote`, `replyBox.addNoteAndSetAs`), alphabetically inserted. **Skipped:** v1.0.3's "Private Note button fix" (re-ordering `expandReply` to emit `set-reply-type` after `nextTick`) is N/A in v2 — the reply box uses two-way `messageType` defineModel + a Tabs component, no expand-then-emit race exists. |
| UX10 | Keyboard shortcuts + copy email + message_to_email automation field + AI formatting | `e66e38bc` | L | – | done f263bf09 + 6b63708d + 0b4baf7f + 7dc437c3 + 9a11a5e6 | Split into five commits. **(a) f263bf09 keyboard shortcuts**: global R / N / `/` / `?` shortcuts in App.vue, gated on no-input-focused + no-modifier + no-Radix-dialog. R/N route through new emitter constants (SHORTCUT_REPLY / SHORTCUT_NOTE) into ReplyBox.vue which flips messageType (useStorage) and focuses the editor; `/` reuses Sidebar.vue's `goToSearch` pattern (clear cached query/results, push timestamped query when already on the route). `?` opens new `KeyboardShortcutsDialog.vue` (shared-ui Dialog primitives, all labels through useI18n, 24 alphabetically-sorted `shortcuts.*` keys). **Skipped vs source**: Esc handler N/A in v2 (no `expandReply` / `isFresh` / collapse-reply concept; fullscreen Dialog already handles Esc via Radix); TextEditor focus('end') + insertContent last-position changes conflict with v2's deliberate EC10 focus('start') and FS8 already handles macro-at-cursor. **(b) 6b63708d customer-reply banner**: extended `handleNewMessageCollision` to branch on message.type — `incoming` from customer surfaces a blue informational banner (`replyBox.customerReply.banner`), `outgoing` from another agent keeps the existing amber blocking banner. Both reset on conv switch + post-send. **(c) 0b4baf7f copy email**: hover-revealed clipboard icon next to the contact email in `ConversationSideBarContact.vue`. `navigator.clipboard.writeText` + 2s green-check feedback; destructive toast on failure. Three new `contact.copyEmail / .emailCopied / .emailCopyFailed` keys. **(d) 7dc437c3 message_to_email**: new automation field that matches `To:` header(s) on the first incoming message, distinct from `inbox_email` (which is just the inbox `from`). Useful when one inbox has aliases (`support@`, `orders@`, …) and routing depends on which the customer wrote to. Backend: `models.MessageToEmail` constant + Conversation field + evaluator case + `get-conversation` query computes via `string_agg(jsonb_array_elements_text(meta->'to'))` from the first `type='incoming'` message. No DB migration (computed column). Frontend: filter exposed in `useConversationFilters.js`; relabeled `inbox_email` from "To email" to "Inbox email" so the two are distinguishable; new `automation.field.toEmailActual` + `automation.field.inboxEmail`, dropped `automation.field.toEmail`. **(e) 9a11a5e6 timestamp inline with sender**: moved the per-message timestamp from a row beneath the bubble into the existing sender-name flex row (gap-2 baseline). Tooltip preserved; saves ~14px per message on a busy thread. **Deferred**: AI-formatting hunk in `cmd/rag.go` + `handleGenerateResponse` in `ReplyBox.vue` — RAG (T3a) isn't ported to v2 yet, will be picked up at T3a port time. Validation-error specificity hunk in `CreateOrEditRule.vue` already covered by v2's `getRulesValidationError()` which returns translated keyed messages (cleaner than the v1.0.3 hardcoded strings). |
| UX11 | Prevent inline pasted images from being deleted by orphan cleanup | `1d68d890` | S | – | done `0c838998` | Pasting/dropping images into the reply editor uploads them with `model_id=NULL`; the 12h `DeleteUnlinkedMedia` job then reaps any messages-typed media that's been NULL-linked for 24h, breaking inline images for slow-typing agents and resumed drafts. Three fixes ported: (1) new `attach-by-uuid` query + `AttachByUUID` manager method + `mediaStore` interface entry, used by `InsertMessage` to scan outbound HTML for `/uploads/<uuid>` references and link them to the just-inserted row; the SQL only updates rows where `model_id IS NULL` so it can't poach media already attached elsewhere. (2) Orphan-cleanup `get-unlinked-message-media` extended with `disposition != 'inline'` as a belt-and-braces guard. Note: in Postgres `disposition != 'inline'` evaluates to NULL (falsy) for NULL-disposition rows, so they won't be cleaned either; in practice every upload path through `cmd/media.go` sets disposition explicitly to `attachment` or `inline`, so the only NULL-disposition rows are theoretical. (3) Regex matches strict UUIDv4 shape so arbitrary `/uploads/foo.txt`-style paths can't accidentally rewrite. **Skipped:** v1.0.3's `refreshContentUploadURLs` subfix (regenerates fresh signed URLs for `/uploads/UUID` references on every API fetch) targets a different bug — vue-letter renders content in an `<iframe srcdoc>` which doesn't share session cookies, so signed URLs expire mid-session. v2 has the signed-URL infrastructure but the iframe cookie issue is a display-time concern separate from orphan cleanup; if it surfaces in v2 it should be tracked as its own unit. |
| UX12 | Default Previous Conversations accordion to open | `b5c3d2a2` | S | – | done 8195b4df | One-line change to `ConversationSideBar.vue`: seed the `useStorage('conversation-sidebar-accordion', [...])` default with `['previous_conversations']` so the section is expanded on first load. Existing users keep their stored state because `useStorage` only consults the default when the key is absent. localStorage key is kebab-case (`conversation-sidebar-accordion`) which diverges from the SKILL.md camelCase convention, but the key already existed in upstream v2 unmodified — renaming would silently lose every user's current collapse state, so left as-is. |
| UX13 | Always mount InboxView so list data loads | `77330b8c` | S | – | done (already in v2) | The v1.0.3 bug was caused by `v-if` on the router-view that mounts `InboxView`: when no conversation was open, the router-view was destroyed and `InboxView.onMounted` (which calls `fetchConversationsList`) never ran. v2.1.1 baseline (card view) avoids this entirely because the detail panel is always mounted in its own `ResizablePanel`. The table-view addition (`20eb7d1b`) introduced single-pane swapping but used `v-show` (not `v-if`) on both the list and detail divs from day one, so the detail-side `<router-view>` stays mounted (just `display:none`) when no conversation is selected — `InboxView` mounts, the fetch fires, and the list populates. Vue Router's nested-route resolution keeps `InboxView` as a parent wrapper even when navigated to `conversation/:uuid`, so it never unmounts when switching between list-only and conversation-open states either. Verified by re-reading `InboxLayout.vue`, the route table in `router/index.js`, and `InboxView.vue`'s `onMounted` data flow. No code change required. |
| UX14 | Full-width list/detail toggle + full-width messages | `f2cad976` | M | – | done (already in v2) | Both halves landed in v2 ahead of the port. (a) **List/detail toggle**: `InboxLayout.vue` already swaps to single-pane (list-only or detail-only with a "Back to list" button) when `viewMode === 'table'`. Same pattern v1.0.3 gated on the Freshdesk theme — v2 gates on the user's view-mode preference (toggle in `ConversationList.vue`, persisted via `useStorage('conversationViewMode')`). Shipped in `20eb7d1b` (Add table view). (b) **Full-width messages**: `themes/fresh/theme.scss` lines 143-148 already include `[data-theme="fresh"] .w-4\/5 { width: 100% !important; }` — verbatim of v1.0.3's `.theme-freshdesk .w-4\/5` rule from sibling commit `4ca54412`. Shipped in `3508730c` (Add Fresh theme). The v1.0.3 commit message also mentioned "removes double scrollbar"; v2's Fresh theme already addresses that via the `.fresh-unified-scroll` rules at lines 126-141 of the same file. No code change required. |
| UX15 | Make Fresh theme default + improve typography | `0b390983` | S | – | done 965918f2 | Two-part source commit — typography slice was already on v2 (landed with the original Fresh-theme port `3508730c`, lines 206-251 of `themes/fresh/theme.scss`), and v2's version is better than v1.0.3's: `margin-bottom: 0` plus `p:empty / p:has(br:only-child) { min-height: 1em }` spacer pattern, vs v1.0.3's fixed `0.65rem` paragraph margin. Only the storage-default flip needed porting. v2's `useTheme.js` falls back to `DEFAULT_ID = 'default'` when localStorage is empty; introduced a separate `INITIAL_ID = THEMES.some(t => t.id === 'fresh') ? 'fresh' : DEFAULT_ID` so new users land on Fresh while existing users keep their selection (`useStorage` only consults the default arg when the key is absent). Validation fallback stays as `DEFAULT_ID` so an invalid stored id reverts to upstream-stock. **Skipped:** the `freshdesk → fresh` localStorage migration in the source commit is a v1.0.3-only artifact (theme was renamed mid-development in `137c9377`); v2 only ever shipped `'fresh'`. |
| UX16 | Relative timestamps in lists/headers | subset of `13c8244e` | S | – | done be7a89c4 | Spec framing was off — the `datetime.js` half of `13c8244e` actually targets **message bubbles**, not list rows or the conversation header. Lists/header already use `getRelativeTime` (RA1) for the compact `5m / 2h / 3d` form. Ported the verbose-relative prefix on `formatMessageTimestamp` ("2 days ago (Mon, 3 Mar 2026 at 7:50 AM)") and the matching long-form `formatFullTimestamp` used in the hover tooltip. Older-than-30-day messages skip the relative prefix and show only the long-form date. The `differenceInMinutes/Hours/Days` imports were already in shared-ui via `getRelativeTime`. CID half of the source commit is ER7 (done already in v2). |
| UX17 | Show error toast and restore editor content when send fails | `d94cd572` | S | – | done 6d87e48a | Bundled with EC2 — same commit. |
| UX18 | Notifications, contact mgmt, team inbox UI fixes | subset of `9513bd98` | M | – | done d602105f + 52ebe8e1 + 0f019e31 | Split into three commits — the residual non-followers slice of the same bundle that fed UX5. **(a) d602105f contact swap + quick-create + name-search expansion**: new `PUT /conversations/{uuid}/contact` endpoint (perm `conversations:update_status`); reuses v2's existing `CreateContact` upsert path for `POST /contacts/quick` (perm `contacts:write`) instead of v1.0.3's thinner `CreateContactSimple`; expanded `search-contacts` SQL to email OR first_name OR last_name; added missing `id` field to `ContactResult` (the SQL was already selecting it but the struct dropped it on scan). Sidebar UI: hover-revealed swap icon next to contact name, search-by-name-or-email picker with inline quick-create form, optimistic update + WS broadcast via `BroadcastConversationUpdate`. `email-recipients.js` reshape detects when the conversation contact has been swapped (lowercase compare against `contactEmail`) and substitutes the current contact in TO instead of echoing the stale meta from-address; also strips anything from CC that's already in TO so post-swap addresses don't duplicate. Six new i18n keys, alphabetically sorted. **(b) 52ebe8e1 UI polish**: SimpleTable drops `table-fixed` so columns size to content (macros admin no longer over-allocates); ActivityLog side padding shrunk from `xl:px-56` → `xl:px-24` (1080p was wasting ~70% of the viewport); SelectTag CommandList gains `max-h-48 overflow-y-auto` so a teams/agents list >12 entries no longer pushes off-screen. **(c) 0f019e31 contact note notify**: new `NotificationTypeContactNote`; `createContactNoteReq` accepts optional `notify_user_ids`; `handleCreateContactNote` dispatches in-app + email per recipient (off-goroutine so SMTP can't block the response). App struct gains a `notifDispatcher` field so cmd-layer handlers can fire notifications without going through the conversation manager. Two i18n keys for subject/title; uses `stringutil.HTML2Text` for the in-app body excerpt (vs v1.0.3's inline stripHTML). Frontend recipient picker deferred — v1.0.3 source ships backend-only too. **Skipped from the bundle**: subject inline edit (already in v2 — `cmd/conversation.go:handleUpdateConversationSubject` + override `Conversation.vue` sticky-header edit), team-inbox-show-all setting (already covered by v2's permission-based design — `PermConversationsReadTeamAll` is a per-role grant which is more flexible than v1.0.3's global toggle), per-inbox AI settings (deferred to T3h since it depends on the RAG infrastructure that's still pending), `TmplNoteAdded` private-note notify branch (UX5 deliberately scoped notifyParticipants to customer-replies-only to avoid the every-agent-reply-notifies-every-follower noise problem), collision warning yellow inline-style override (v2 already uses Tailwind amber classes with proper i18n which is functionally identical). Latent v1.0.3 bug noted not ported: v1.0.3's `insert-contact-simple` ON CONFLICT clause has a typo on the last_name update — the CASE checks `EXCLUDED.first_name != ''` for the last_name column instead of `EXCLUDED.last_name != ''`. v2 path doesn't share that code so no port needed; worth flagging when patching the v1.0.3 fork. |
| UX19 | Conversation total count on views | upstream `4e7eeafd` (check if fork-specific delta) | S | – | done (already in v2) | Source `4e7eeafd` is on `v2.1.1-plus-enhancements` already (verified via `git branch --contains`) and the count is rendered in `frontend/apps/main/src/features/conversation/list/ConversationList.vue` in two places: (a) view-mode fallback (`v-else` when `route.params.viewID` is truthy) shows `conversationStore.conversations.total` in a ghost Button, faithful to the upstream commit; (b) v2 also surfaces the same total inline next to the status filter dropdown's `listStatusLabel` for non-view routes (extension landed alongside the multi-status filter / FS1 work). No fork-specific delta to port — the fork's `v1.0.3-plus-enhancements` only contains the upstream commit itself, no further refinements. No code change required. |
| UX20 | Refresh stale message cache on revisit | n/a (new) | S | – | done 28ded35c | Currently `messages.data.invalidate(uuid)` only fires for non-open conversations. A conversation opened in a tab from a previous session shows stale state on revisit until hard-refresh. Fix: on `conversationOpened`, if the cached `last_message_at` is older than the server's, invalidate + refetch. **Implementation**: `fetchMessages` in `stores/conversation.js` now consults a new `isMessageCacheStale(uuid, cachedMessages)` helper before the early-return on cache hit. The helper compares the row's `last_message_at` from `conversations.data` (the list, kept current by WS-driven `mergeConversationUpdate` / `updateConversationLastMessage` and by manual list refetch) against the newest cached message's `created_at` (the cache's tail, since `getAllPagesMessages` returns ascending). If server is ahead, calls `messages.data.purgeConversation(uuid) + incrementMessageVersion()` and falls through to the normal fetch path. NaN guards on both timestamps; missing-data on either side is treated as fresh (conservative — deeplink visits to conversations not in the current list keep prior behaviour). The fetchNextPage path is untouched. |
| UX21 | Customer's full ticket history on contact detail page (with tabs for Notes/Conversations) | `a427efc2` + refinement `d8db92c9` | S | – | done 6f7486ca | New `GET /api/v1/contacts/{id}/conversations` endpoint (perm `contacts:read`, limit 100) wrapping the existing `GetContactPreviousConversations` manager method. New `ContactConversations.vue` view mirrors the sidebar `PreviousConversations.vue` styling but fetches via API, not via the conversation store. `ContactDetailView.vue` wraps `ContactNotes` and `ContactConversations` in shared-ui `Tabs` (Conversations is `default-value`; Notes tab hidden when `!can('contact_notes:read')`, matching the existing visibility guard). Adapted for v2 monorepo: `@shared-ui` imports, `globals.terms.note` plural form instead of live's hardcoded `'Notes'` fallback (live referenced a non-existent `globals.terms.notes` key, only worked because of the literal fallback arg). Added one new pluralised i18n key `conversation.sidebar.previousConvo.count`. |
| UX22 | Activity notes for trash / restore / spam / not-spam status changes | `48c5cb57` | S | 5.0 Spam&Trash | done (covered by IP7 711a7f40) | Until now, `MoveToTrash` / `RestoreFromTrash` / `MarkAsSpam` / `MarkAsNotSpam` updated the DB row silently — no audit trail of who did what. Update each manager method to take an `actor umodels.User` parameter and call `RecordStatusChange(<status>, uuid, actor)` after the DB update. Update the four cmd handlers to pass `user` through. Auto-spam path in `processIncomingMessage` (IP7's gate) passes a zero-value `umodels.User{}`; `MarkAsSpam` skips activity recording when `actor.ID == 0` since the IMAP source folder is implicit context. Closed/Resolved already get notes via the existing `UpdateConversationStatus → RecordStatusChange` path. **v2 port verification (UX22 sweep):** all four manager methods at `internal/conversation/conversation.go:2217-2233` already accept `actor umodels.User` and delegate to `UpdateConversationStatus`, which calls `RecordStatusChange(status, uuid, actor)` at line 1007 — so every audit-log path covered by UX22 is live. All four `cmd/conversation.go` handlers (`handleMoveToTrash`, `handleRestoreFromTrash`, `handleMarkAsSpam`, `handleMarkAsNotSpam`) pass the resolved `user` through. Auto-spam path in `processIncomingMessage` (`internal/conversation/message.go:1064`) passes the looked-up `systemUser` rather than a zero-value `umodels.User{}` — a small enhancement over the v1.0.3 source (activity log will read "System marked the conversation as Spam" with a real actor instead of being skipped). No code commit needed; doc-only update. |
| UX23 | Macro picker readability — folder/title split, wider modal, scrollable dropdowns | `37f97d1f` + macro-list refinement included | S | EC15 | done f4a7a1bd | The Ctrl+K → Apply macro picker had hard-to-scan rows because macros are imported with `[Folder] Title` names, the prefix had the same visual weight as the title, and titles wrapped to 3-4 lines. In `CommandBox.vue` parse the prefix client-side (regex `/^\[([^\]]+)\]\s*/`) and render the folder as a small uppercase muted label above the title; title is single-line `truncate` with `:title=` attr for overflow. Bump list column from `col-span-4` to `col-span-5`. Tighten padding `py-3 → py-2`, smaller Zap icon. Modal width changed from `!min-w-[50vw]` to `!w-[80vw] !max-w-[80vw]` for breathing room. Helpers `macroFolder(label)` / `macroTitleOnly(label)` co-located with the picker. |
| UX24 | Submit & Set Status dropdown overflow scroll | (already covered in EC20 refinement `278d3bdd`) | S | EC20 | done 7a1a733c | `DropdownMenuContent` for the new-conversation Submit picker has `max-h-[60vh] overflow-y-auto` so installs with many statuses don't push the dropdown off-screen. Bundled into EC20's port commit. |
| UX26 | Team badge colour + initial-letter avatar (alternative to emoji) | `ec2891cd` (deploy auto-commit; team-form + schema hunks) | S | – | done ad8a5110 | Adds `color TEXT` column on `teams` table (v2 has `emoji` only). Admin team form gets a colour picker alongside the existing emoji picker; description: *"When set, the team shows a coloured square with the first letter of the team name (instead of the emoji)."* Team avatars render as: emoji if present → coloured letter badge if `color` set → fall back to plain name. Migration v2.2.11 adds the column + a `^#[0-9a-fA-F]{6}$` CHECK constraint idempotently (ADD COLUMN IF NOT EXISTS + a DO block guarding the constraint via pg_constraint lookup). schema.sql carries the matching constraint for fresh installs. Backend `Manager.Create`/`Update` take an extra `color` arg; queries use `NULLIF($n, '')` so the Clear button writes SQL NULL rather than tripping the CHECK regex. Frontend: `teamFormSchema.js` makes `emoji` optional and adds a hex regex on `color`; `TeamForm.vue` gets a flex row with `<Input type="color">` swatch + free-text hex input + Clear button + live letter-badge preview, both inputs synced via `setFieldValue`. Sidebar Team Inboxes (`team.color` flows through `users.teams` → `get-user-teams`) and admin teams DataTable both honour the new precedence. Conversation list quick-assign chips and conversation detail header in v2 are text-only (no team avatar rendered there to extend), so no edits needed on those surfaces — flagging as a v2-vs-v1.0.3 design delta rather than missing port. Five new alphabetically-ordered i18n keys (`admin.team.color`, `.color.clear`, `.color.description`, `.color.invalidHex`, `admin.team.emoji.description`); en.json only, Crowdin syncs the rest. |
| UX25 | Activity-trail coverage for every status change (incl. background trash worker) | `22287f7c` | S | UX22, 5.0 Spam&Trash | done d8dac1ab | **Audit-trail completeness pass.** Background: a historical bug pre-2026-04-28 (verified via DB query: ~600 closed conversations between 2026-04-13 and 2026-04-27 missing `type='activity'` + `content ILIKE '%Closed%'` rows; coverage flipped to ~100% from 2026-04-28 onward). Root cause was bulk-close paths in v1.0.3 silently bypassing `RecordStatusChange`; it's already fixed in current main paths but two gaps remain: **(a)** `auto-trash-old-resolved` and `auto-trash-old-spam` queries in `internal/conversation/trash_manager.go` updated `status_id` in bulk via `Exec` with no per-conversation activity record. **(b)** UX22 gaps fixed earlier today (manual trash/spam/restore handlers). For UX25: change both auto-trash queries to `RETURNING uuid`; switch `Exec` → `Select` to capture affected UUIDs; add `recordAutoTrashActivity(uuids, reason)` helper that fetches the system user via `userStore.GetSystemUser()` and calls `InsertConversationActivity(ActivityStatusChange, uuid, "Trashed — <reason>", systemUser)` per UUID. Activities show as `"System marked the conversation as Trashed — Resolved/Closed retention policy"` (or "...Spam retention policy"). At v2 port time: also audit `MergeConversations` (already covered — the merge path inserts its own "Merged into #X" / "Merged #X into this" activity messages — no change needed) and verify no other code path mutates `status_id` outside of `UpdateConversationStatus` / the trash/spam manager methods. |
| UX27 | useDraftManager: derive textContent from htmlContent on draft load | `0744f1b1` | S | – | done d469097c | When a saved draft loads, `loadDraft` was setting `htmlContent.value = content` but explicitly setting `textContent.value = ''`. Reactive predicates that gate UI controls — `enableSend` in `ReplyBoxContent.vue` and `hasDraftContent` (which flows into `hasDraft` → controls `v-if` on the Delete button in `ReplyBoxMenuBar.vue`) — both check `textContent.value.trim().length > 0`. Result: on draft load the Send button stays disabled AND the Delete-draft button is not even rendered, until the agent presses any key (TipTap's onUpdate then repopulates textContent). One-line fix: `textContent.value = getTextFromHTML(content)` in loadDraft (the helper is already imported and used by `isDraftEmpty`). v2 file: `frontend/apps/main/src/composables/useDraftManager.js` line 189 has the same bug verbatim — confirmed via grep. Port 1:1. |

### 5.10 Tier 3 — RAG / OpenRouter / multimodal AI

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T3a | RAG-powered AI assistant — core (embeddings, knowledge sources, prompt engine) | `d83ef682` | XL | – | done `8bac5bdb` | Split as recommended into 3 sub-commits to keep review tractable: **(a) f2677da4 schema + pgvector + AISettings model** — docker-compose `postgres:17-alpine` → `pgvector/pgvector:pg17`; migration `v2.2.15` enables the vector extension idempotently (failure on stock postgres is a graceful no-op — install proceeds without RAG tables); rag_sources + rag_documents tables with hnsw vector index sized vector(1536) for OpenAI text-embedding-3-small; AISettings struct extended with Enabled/EmbeddingModel/SystemPrompt/MaxContextChunks/SimilarityThreshold (T3v transcription fields preserved); five new default settings rows. **(b) d9b8c865 internal/rag package + ai embeddings + sync coordinator** — Manager (rag.go) owns sources CRUD + AddDocument upsert with SHA256 content-hash dedup + Search via pgvector `<=>` cosine; sync subpackage coordinator runs initial SyncAll on Start() then ticks 1h with three syncers (macro, webpage, file — files skip the periodic loop, content captured at upload); ai.GenerateEmbedding always reaches for stored OpenAI provider (embeddings/Whisper aren't on OpenRouter), 60s budget vs chat's 10s; CompletionWithSystemPrompt bypasses ai_prompts so the RAG handler can feed an assembled prompt; cmd/init.go initRAG/initRAGSync; main.go starts coordinator before conversation.Run, stops it during shutdown. **(c) 8bac5bdb cmd/rag handlers + admin UI + Generate Response button** — 9 handlers (sources CRUD, sync, search, generate, multipart file upload), source ops gated on `ai:manage`, generate on `conversations:write`; `views/admin/ai/RAGSettings.vue` three-section admin (Test Knowledge Base, source list with per-source sync/edit/delete, file upload dialog) adapted from v1.0.3's AdminPageWithHelp to v2's AdminSplitLayout and from raw English to 40+ i18n keys; `ReplyBox.vue` handleGenerateResponse assembles conversation transcript client-side (filter private/activity, sort oldest-first, strip HTML, tag Customer:/Agent:) and calls api.ragGenerate; `ReplyBoxMenuBar.vue` Sparkles-icon button next to the macro picker, animated pulse during request, hidden in private-note mode; new /admin/knowledge-sources route + sidebar entry. **v2 deltas vs v1.0.3:** (i) AISettings model extended in place rather than replacing T3v's minimal struct — RAG and transcription share the `ai.` settings prefix without conflict; (ii) v1.0.3's coexistence with T3b (OpenRouter) was already in the bundle — filtered out, T3b lands separately; (iii) v1.0.3's TextEditor image-handling hunks (paste/drop/inline upload) are T3e territory, dropped from this port; (iv) v1.0.3's contacts:delete permission + SoftDeleteContact + handleDeleteContact + ContactDetailView delete UI dragged along by the bundle but unrelated to RAG — dropped, can land as its own unit if anyone wants it; (v) v1.0.3's SMTP test-email feature (cmd/settings.go handleTestEmailNotificationSettings + NotificationSettingForm.vue test panel) similarly bundled but unrelated — dropped; (vi) media access-check loosening (`ModelID.Valid` guard) was already addressed by v2's UX11 inline-image fixes; (vii) replaced v1.0.3's hardcoded English strings throughout with i18n keys; (viii) used v2's structured logger conventions; (ix) used envelope.NotFoundError for missing sources rather than v1.0.3's bare 404. **Verified:** go build ./...; go test ./... (all packages pass including new internal/rag/* test files); pnpm exec vitest run (130 tests pass); fresh boot with no RAG configured starts cleanly — admin endpoints return 5xx until pgvector is available, generate path drops to empty-context-only when search fails. |
| T3b | OpenRouter provider | `ada2b0c1` | M | T3a | done `acda6a6c` | Adds OpenRouter as a third AI provider alongside OpenAI. `internal/ai/openrouter.go` implements the same `ProviderClient.SendPrompt` interface as `openai.go` so `getDefaultProviderClient` / `Completion` / `CompletionWithSystemPrompt` route through whichever provider is set is_default. Wire format: OpenAI-compatible body + `Authorization: Bearer`, `HTTP-Referer`, `X-Title` headers for OpenRouter usage attribution. 60s timeout matching openai.go's T3f budget. **Backend surface:** `GetProviders` / `GetAvailableModels` / `SetDefaultProvider` / `TestProvider` on `ai.Manager`; 5 new `cmd/ai.go` handlers gated on `ai:manage`; 4 new queries (get-providers, set-default-provider, upsert-openrouter, set-openrouter-config). **Model list:** `FetchOpenRouterModels` lazy-fetches `/api/v1/models` with a 24h in-memory cache + static fallback (~30 models balanced across Anthropic/OpenAI/Google/xAI/Moonshot/Zhipu/DeepSeek/Qwen/Meta/Mistral). **Database:** migration v2.2.16 runs `ALTER TYPE ai_provider ADD VALUE IF NOT EXISTS 'openrouter'` (idempotent, autocommit-safe) then idempotently seeds the OpenRouter row with empty api_key + Haiku 3 default; schema.sql carries matching baseline. **Frontend:** `AISettings.vue` extended with OpenAI + OpenRouter provider cards above the existing T3v transcription form — Configured/Default badges, Save/Test/Set-as-default buttons, model dropdown for OpenRouter. **v2 deltas vs v1.0.3:** **(i)** v2 monorepo paths (`frontend/apps/main/src/...`) and `@shared-ui/components/ui/*` imports. **(ii)** v1.0.3 hardcoded English strings throughout the AISettings page; replaced with 18 new `admin.ai.providers.*` i18n keys. **(iii)** v1.0.3 used `vue-sonner` toast directly; v2 routes through the existing `emitter`+`SHOW_TOAST` pattern (matches T3v + other admin pages). **(iv)** v1.0.3's `getDefaultProviderClient` called `crypto.Decrypt` on the OpenRouter key even though `setOpenRouterConfig` stored it plaintext — a latent bug. v2 stores plaintext AND consumes plaintext so the path works; T3j will flip both sides to encrypted simultaneously. **(v)** T3b shares `views/admin/ai/AISettings.vue` with T3v's transcription form rather than replacing it; one page with Provider cards + Transcription form under `ai:manage`. **(vi)** Default model preserved as `anthropic/claude-3-haiku` (cheapest "good enough" out-of-the-box choice). **(vii)** Existing OpenAI-only path unchanged: with no OpenRouter configured, `getDefaultProviderClient` still returns the OpenAI client (existing T3a state preserved). **(viii)** OpenAI's `UpdateProvider` call ignores the new `model` parameter (fixed at `gpt-4o-mini`). **Verified:** `go build ./...` clean, `go test ./...` all packages pass, `pnpm exec vitest run` 130/130 pass, Vue SFC parse on AISettings.vue: 0 errors. |
| T3c | System prompt + RAG settings UI | `c38fe663` | M | T3a | done `86a0b859` | Frontend-led port: backend was already fully wired by T3a (AISettings struct fields + cmd/rag.go reads SystemPrompt with defaultRAGSystemPrompt fallback + v2.2.15 migration seeds the keys). Adds a new "AI Assistant Settings" card to `frontend/apps/main/src/views/admin/ai/AISettings.vue` with Textarea (system prompt), Input (max context chunks 1..50), Input (similarity threshold 0..1). Loads via the existing `getSettings('ai')` envelope (renamed `loadTranscriptionSettings` → `loadAISettings` and folded both forms' state into one GET); saves via `updateSettings('ai', {...})` with only the three RAG keys. **Backend reworked partial-save merge** (`cmd/settings.go handleUpdateAISettings`): pre-fetches current AISettings into the request struct, then `Decode` overlays only the JSON fields present in the body. Without this, T3v transcription form + T3c RAG form would clobber each other with zero defaults on save (latent bug in T3v's standalone form, surfaced by adding the second card). Numeric ranges validated server-side. **v2 deltas vs v1.0.3 c38fe663:** **(i)** v2 monorepo paths + `@shared-ui` imports. **(ii)** 11 new `admin.ai.rag.*` i18n keys (v1.0.3 hardcoded English); double-curly placeholders escaped vue-i18n style (`{'{{site_name}}'}`). **(iii)** Default `similarity_threshold` = 0.25 (matches the v2 production-tuned value baked into rag.go fallbacks, search handler default, v2.2.15 seed) — v1.0.3 had 0.7 in the form but 0.25 in the runtime, a UI/runtime mismatch this port doesn't carry forward. **(iv)** Toast via existing `emitter`+`SHOW_TOAST` (matches T3b + T3v) instead of v1.0.3's vue-sonner direct usage. **(v)** Server-side range validation (v1.0.3 had none). **(vi)** Partial-save merge generalises v1.0.3's API-key-only preservation; v2's AISettings has no API keys (those moved to `ai_providers` in T3a/T3b) so the password-field special case is replaced by a struct-overlay decode. **(vii)** v1.0.3's `max_context_chunks` default was 5 (kept); range cap 50 (v1.0.3 had 20) — production token budgets at gpt-4o tier comfortably handle ~50 chunks. **Verified:** `go build ./...` clean, `go test ./...` all packages pass, `pnpm exec vitest run` 130/130 pass, Vue SFC parse on AISettings.vue: 0 errors, i18n JSON parses 11 new RAG keys cleanly. |
| T3d | External search API integration | `0da66067` | M | T3a | done `6ed6399a` | Two-step classify-then-search: (a) `classifySearchIntent` asks the LLM to bucket the customer message into product/category/faq intents (pure chit-chat returns empty list, short-circuits); (b) `performExternalSearch` fans out one POST per matched intent to `{base_url}{endpoints[type]}`, formats hits as Markdown-ish sections, joins them. Substituted into the system prompt via new `{{external_search_results}}` placeholder (default prompt unchanged — custom prompts opt in by including the token). Settings extend `AISettings` with `ExternalSearch{Enabled,URL,MaxResults,Endpoints,Headers}`; v2.2.17 + schema.sql seed all five as off/empty so upgrades are no-op. `Endpoints`/`Headers` stored as JSON-string fields (intent→path; header→value) because v1.0.3 UX accepts arbitrary keys and a typed schema would force migrations on every new intent type; parse failure at use-time degrades to "no external search" so admins can save half-typed JSON. Admin UI gets a new External Search Integration card between T3c's RAG card and T3v's transcription form, behind the toggle. **v2 deltas vs v1.0.3 0da66067:** **(i)** v2 monorepo paths + `@shared-ui` imports. **(ii)** v1.0.3 used a bare `http.Client{Timeout:10s}` per call. v2 reuses the SS2 ssrfguard'd pattern from `internal/auth/auth.go` + `internal/webhook/webhook.go` — `initExternalSearchClient` in `cmd/init.go` reads `ai.allowed_hosts` CIDRs, builds an `ssrfguard.New(...).Control` dialer, parks the client on `App.extSearchClient`. Admin-supplied URLs can no longer reach loopback / RFC1918 / link-local / IPv6-reserved targets without explicit allowlist. SSRF dial errors wrapped to `"blocked by SSRF guard: ..."` in the warn log. **(iii)** 17 new `admin.ai.externalSearch.*` i18n keys (v1.0.3 hardcoded English); vue-i18n placeholder-escape style for `{{external_search_results}}`. **(iv)** Toast via existing `emitter`+`SHOW_TOAST` (matches T3b/T3c/T3v) instead of v1.0.3's `vue-sonner` direct usage. **(v)** Reuses T3c's partial-save merge in `handleUpdateAISettings` — new fields flow through cleanly because `AISettings` has no `omitempty` tags; added server-side range check `ExternalSearchMaxResults` ∈ [0,10] matching the UI cap. **(vi)** Customer message wrapped in `<customer_message>` tags inside `classifySearchIntent` (T3l prompt-injection mitigation; v1.0.3 0da66067 had this on the classifier already). **(vii)** v1.0.3's `stripHTML` was the buggy O(n²) loop-replace in `cmd/rag.go`; v2's regex-based equivalent in `internal/rag/sync/macro.go` is package-internal — kept a small purpose-built `stripHTMLForExternalSearch` in `cmd/rag.go` for the ~200-char product blurb site rather than exporting macro.go's. **(viii)** New `[ai] allowed_hosts = []` block added to `config.sample.toml` + `config.local.toml`. **Verified**: `go build ./...` clean, `go vet ./...` clean, `go test ./...` all packages pass, `pnpm exec vitest run` 130/130, en.json parses clean (17 new keys), Vue SFC parse 0 errors. **Smoke**: disabled (default) → no behaviour change, `{{external_search_results}}` → ""; enabled-but-unreachable URL → graceful warn-and-continue; private-IP URL → ssrfguard denies, "blocked by SSRF guard" log line, RAG continues with pgvector-only context. |
| T3e | Multimodal image support (resize → AI provider) | `963b4900`, `ba6f9247`, `21aa7983`, `c5844b1d` | L | T3a | done `53f21b03` | Bundle of all four upstream commits — single logical unit (resize util → conversation image extraction → multimodal payload type → RAG handler integration). **What lands:** **(a)** new `internal/image/resize.go` with `ResizeForAI` / `ToBase64DataURL` / `ResizeAndEncodeForAI` — caps long side at `MaxAIDimension`=500px via `imaging.Fit`, preserves aspect ratio, normalises encoding (EXIF rotation lands as actual pixel rotation). PNG/GIF round-trip native; everything else (image/webp, image/bmp …) falls through to JPEG so unknown `image/*` mimetypes still produce a payload OpenAI's `image_url` accepts. **(b)** `internal/rag/rag.go GetConversationImages` + `MediaBlobFunc` opt. Joins `media → conversation_messages` on `model_type='messages'` (existing v2 polymorphic-association convention), filters `content_type LIKE 'image/%'`, orders newest first, capped at `maxImages` (default 3 — `DefaultMaxRAGImages` in cmd/rag.go). Per-image failures log + skip; nil `mediaBlobFunc` returns (nil, nil) so test/sandbox setups don't trip nil-deref. **(c)** `internal/ai/provider.go PromptPayload.Images []ImageContent` — new field, `omitempty`, so existing text-only wire shape is byte-for-byte unchanged when no images attached. **(d)** `internal/ai/openai.go` + `openrouter.go SendPrompt` branch on `len(payload.Images)`: zero → existing string-content user message; non-zero → array-content with `text` + `image_url` parts. `detail:"low"` maps to flat 85 tokens/image regardless of size — right call given internal/image already capped at 500px. OpenRouter passes the OpenAI shape through to whichever upstream the admin selected (T3b note (i)) — Anthropic Claude 3.x, Gemini, GPT-4o all accept it; vision-incapable models ignore image parts and complete against text alone. **(e)** `internal/ai/ai.go CompletionWithPayload` (new) — promoted to the "real" implementation; `CompletionWithSystemPrompt` now delegates so the text-only call site (classifySearchIntent in T3d) is a one-liner. **(f)** `cmd/rag.go handleRAGGenerateResponse` calls `GetConversationImages` when `conversation_id > 0` (admin "Test Knowledge Base" path skips cleanly with `conversation_id=0`), shapes into `[]ai.ImageContent`, appends a directive to the system prompt when images are present ("Note: The customer has attached images …"), routes through `CompletionWithPayload`. Image-extraction failure does NOT fail the generate call — falls back to text-only. **(g)** `cmd/init.go` + `cmd/main.go` — `initRAG` takes `*media.Manager` and wires `media.GetBlob` as the `rag.MediaBlobFunc`. **Tests:** new `internal/image/resize_test.go` (7 cases — downscale, pass-through, JPEG fallback for unknown content-types, GIF round-trip, decode error, base64 framing); `internal/rag/rag_test.go +TestGetConversationImagesNilMediaBlobFunc` asserts the graceful-degradation contract. **v2 deltas vs v1.0.3:** **(i)** v2 monorepo paths intact (no change). **(ii)** v1.0.3 ba6f9247 included an unrelated whitespace deletion between `initRAG` and `initRAGSync` — not carried. **(iii)** v1.0.3 c5844b1d flipped one typographic apostrophe ('don't' → 'does not') in the inline default prompt; v2's `defaultRAGSystemPrompt` already uses the typographically clean form (T3a). **(iv)** TextEditor paste/drop/inline image upload (flagged in T3a spec notes as "T3e territory") is in a separate v1.0.3 commit not in this bundle — the four upstream commits themselves contain zero frontend changes; remains unported (out of scope). **(v)** v2's `CompletionWithSystemPrompt` preserved as thin delegator rather than removed, so existing T3d `classifySearchIntent` call site doesn't churn. **Verified:** `go build ./...` clean, `go vet ./...` clean, `go test ./...` all packages pass (image: 7/7 new + rag: 5/5), `pnpm exec vitest run` 130/130 pass. **Smoke (logical):** no image attachments → behavior unchanged (text-only prompt, existing wire shape preserved by Images omitempty + len() branch). With image attachments + OpenAI selected → multimodal `image_url`+`text` shape. With OpenRouter selected → identical shape passed through to upstream model. `conversation_id=0` (admin Test Knowledge Base) → image extraction skipped. |
| T3f | RAG context limiting + frontend timeouts | `d986a684`, `d7d34fb6` | S | T3a | done `f31d8f19` | Three fixes to bound prompt size + AI provider + token cost on long email threads. **(a)** `cmd/rag.go handleRAGGenerateResponse`: cap `req.CustomerMessage` at 6000 chars (keep tail / "[Earlier messages truncated]" head marker / log original+truncated lens). **(b)** `internal/ai/openai.go NewOpenAIClient`: chat client timeout 10s → 60s; embedding path already had its own 60s budget. **(c)** `frontend/apps/main/src/api/index.js ragGenerate`: explicit `{ timeout: 60000 }` (was inheriting 10s http default — T3a port did NOT set this; spec narrative there was inaccurate). **(d)** `frontend/.../features/conversation/ReplyBox.vue handleGenerateResponse`: `.slice(-10)` after sort so we don't ship a 200-message thread before backend cap kicks in. **v2 deltas vs v1.0.3:** **(i)** v1.0.3 d986a684's stripHTML "insert spaces between tags" fix N/A — v2's `internal/rag/sync/macro.go stripHTML` is regex-based and already inserts spaces; T3l covered the package-level regex hoist + 100KB input cap on the same function. **(ii)** v1.0.3 d986a684's per-stage `TIMING rag_search / ecommerce / external_search / before_ai_completion / ai_completion_done / ai_completion_failed` log lines were diagnostic-only (left in during a v1.0.3 ecommerce performance investigation); v2's logging conventions prefer structured fields over ad-hoc TIMING prefix strings; not ported. The existing "RAG generate response" structured log covers the success case. **(iii)** v1.0.3 d986a684's `testEcommerceCustomer` / `testEcommerceOrder` 30s-timeout endpoints belong with T3p (Maho provider) and T3ae (Maho API Platform v2 migration); out of scope. **(iv)** v1.0.3 d7d34fb6 also bumped `openrouter.go` + `provider.go FetchOpenRouterModels` to 60s; v2 doesn't have OpenRouter yet (T3b pending). **Verified:** `go build ./...` clean, `go test ./...` all packages pass, `pnpm exec vitest run` 130/130 pass. |
| T3g | RAG context quality improvements | `30b5194c` | S | T3a | done `7485d70b` | Three fixes to reduce outdated/irrelevant AI responses on Generate Response, mirroring v1.0.3 30b5194c. **(a) Per-message ISO timestamps + most-recent marker** (`ReplyBox.vue handleGenerateResponse`): each message in the transcript is prefixed `[<en-AU formatted date>] Customer/Agent:` and the final public message gets a `[MOST RECENT MESSAGE — respond to this]` tag. Lets the LLM reason about recency vs treating all context as equally fresh. **(b) Private notes as internal-only context block** (`ReplyBox.vue`): the prior `.filter(m => !m.private)` dropped all internal notes; now public messages (last 7) and private notes (last 5) are partitioned, public text built first, private notes appended under a `--- INTERNAL AGENT NOTES (not visible to customer — use to inform your tone and response):` header. Agent guidance like "we should apologise, there was a booking glitch" now reaches the LLM. Public cap reduced 10 → 7 to leave room for the notes block within T3f's 6000-char server cap. **(c) `{{today}}` substitution in system prompt** (`cmd/rag.go`): default `defaultRAGSystemPrompt` gains a `Today is {{today}}.` line, and a new substitution expands `{{today}}` to `Weekday D Month YYYY` in `time.FixedZone("AEST", 10h)` immediately before the existing `{{site_name}}` / `{{context}}` / `{{macros}}` / `{{enquiry}}` substitutions. Custom admin-saved prompts can opt in by including `{{today}}` in their template. **v2 deltas vs v1.0.3:** **(i)** v1.0.3 patched the inline-string default prompt directly because the default lived as a literal in the handler; v2's default sits in package-level `defaultRAGSystemPrompt` (introduced at T3a), so the prompt-edit and substitution-edit are two distinct hunks rather than one combined change. **(ii)** AEST hardcoded to match v1.0.3 source — defer multi-tz handling to a future enhancement; the substitution site is the single edit point if/when it lands. **Verified:** `go build ./...` clean, `go test ./...` all pass, `pnpm exec vitest run` 130/130 pass. |
| T3h | Per-inbox AI settings | subset of `cf97cb06` | M | T3a | done `083dc000` | Filtered subset of cf97cb06 — ports per-inbox AI overrides only; the bundle's SKU-stock-data (T3u territory), Meilisearch multi-search format, ecommerce-context integration (T3r), email-aliases inbox.Config field, smart-contact-detection, automation TagsInput/single-group fixes, reply/private-note button mode fix, discard-draft confirm, bulk-close, full-height assign dropdown, table view group-rename, contact-note agent notifs, and HTML-sanitisation relaxation are all left for their own rows. **Storage shape:** new `inbox_ai_settings` table (not JSONB on inboxes), one row per inbox with FK ON DELETE CASCADE on inboxes(id) + UNIQUE(inbox_id) for the upsert ON CONFLICT target. Columns mirror the runtime-relevant subset of AISettings (system_prompt, max_context_chunks, similarity_threshold, external_search_*, knowledge_source_ids JSONB array). Voicemail transcription / embedding model are intentionally excluded — transcription runs at message ingest before any inbox reasoning happens, and embedding model is chosen at the AI manager not the RAG handler. **Migration v2.2.18** (idempotent CREATE TABLE IF NOT EXISTS) + matching schema.sql baseline; registered in cmd/upgrade.go. **Backend surface:** `setting.Manager.GetInboxAISettings` / `UpsertInboxAISettings` / `DeleteInboxAISettings` / `GetEffectiveAISettings(inboxID)`. The Effective method projects the global AISettings into an InboxAISettings shape on the no-row-found path so call sites are inbox-id agnostic. New `conversation.Manager.GetConversationInboxID` for the inbox lookup from a conversation. **3 cmd handlers** (GET/PUT/DELETE `/api/v1/settings/ai/inbox/{id}`) gated on `ai:manage`, mirror the global handler's range checks. **RAG generate path** (`cmd/rag.go handleRAGGenerateResponse`) accepts an optional `inbox_id` body field, falls back to `GetConversationInboxID(conversation_id)` when absent, then reads via `GetEffectiveAISettings(inboxID)` — admin "Test Knowledge Base" still works (passes neither, resolves to global). **`internal/rag/rag.go Search`** signature extended with variadic `sourceIDs ...int`; non-empty triggers an `AND source_id = ANY($4)` clause via `pq.Array`, empty preserves the pre-T3h all-sources behaviour. **`performExternalSearch`** signature flipped from `settingmodels.AISettings` to `settingmodels.InboxAISettings` — the three fields it reads (URL/Endpoints/Headers) have identical semantics on both types and the global-fallback path projects via GetEffectiveAISettings. **Frontend:** new `Inbox scope` card at the top of the AI settings RAG section with `Select` dropdown (global / per-inbox) + Reset-to-global button when an override exists; per-inbox knowledge-source picker (checkbox list) inside the RAG card, only visible in inbox scope. Save buttons on the RAG card and External Search card dispatch by scope: global writes via the existing partial-merge `/api/v1/settings/ai`, per-inbox writes the full InboxAISettings via `/api/v1/settings/ai/inbox/{id}`. Inbox-to-global flips restore a snapshot of the global form so admins don't see stale per-inbox values bleed into the global view; inbox-to-inbox flips re-fetch the row. 13 new `admin.ai.inboxScope.*` i18n keys. **v2 deltas vs v1.0.3 cf97cb06 portion:** **(i)** v2 monorepo paths (`frontend/apps/main/src/...`) + `@shared-ui` imports + `emitter`+`SHOW_TOAST` toast pattern. **(ii)** v1.0.3 stored InboxAISettings created_at/updated_at as `string`; kept the same shape in v2 for JSON-encoding compatibility (v1.0.3 frontend reads them as strings). **(iii)** v1.0.3 hardcoded English strings throughout AISettings.vue's per-inbox additions; replaced with i18n keys. **(iv)** v1.0.3 used `confirm()` directly; v2 keeps that (no shared confirm-dialog primitive on this page yet — matches the page's existing patterns). **(v)** v1.0.3 split the inbox dropdown into the RAG card header alongside the title; v2 promotes it to its own dedicated `Inbox scope` card so the description copy can give richer feedback (override active vs fallback). **(vi)** Server-side range checks on per-inbox upsert mirror the global handler (v1.0.3 had none on the per-inbox path). **(vii)** v1.0.3's GetEffectiveAISettings fell back to global with KnowledgeSourceIDs zero-value (`null`); v2 explicitly sets `[]byte("[]")` so downstream Unmarshal of the array always succeeds without nil-check. **(viii)** Multi-search Meilisearch format from cf97cb06 NOT ported — that's part of T3u/T3r work, the v2 external search remains single-search per intent. **(ix)** v1.0.3 logged `External search context content` (full hit body) for debugging; not carried over — adds noise without ongoing diagnostic value. **Verified:** `go build ./...` clean, `go vet ./...` clean, `go test ./...` all packages pass (cmd, conversation, rag, setting/models, etc), `pnpm exec vitest run` 130/130 pass, en.json parses cleanly with 13 new keys, Vue SFC parse on AISettings.vue: 0 errors. **Smoke (logical):** without per-inbox row → `GetEffectiveAISettings(inboxID)` returns projected global settings, RAG search has no source filter, behaves exactly as pre-T3h. With per-inbox row → effective settings reflect inbox values, search filtered by `source_id = ANY([...])` when knowledge_source_ids non-empty. Admin "Test Knowledge Base" path passes `conversation_id=0` and no inbox_id → resolves to global. Conversation-without-inbox-id-resolution failure logged + falls back to global. Reset-to-global → DELETE row, frontend restores snapshot. |
| T3i | Allow agents (not just admins) to use Generate Response | `bdc59524` | S | T3a | done `361dc0aa` | Backend-only: rag/generate route flipped from `conversations:write` to `messages:write`. |
| T3j | Encrypt OpenRouter API key in DB | `91e0d610` | S | T3b | done `484f7604` | Security: mirror OpenAI key encryption. **Two-sided change** in `internal/ai/ai.go`: (a) `setOpenRouterConfig` wraps the api_key in `crypto.Encrypt` before persisting — the empty-string guard is preserved so the SQL `set-openrouter-config` CASE-WHEN sentinel ("preserve existing key" when $1 = '') still works (encrypting "" would produce non-empty ciphertext and clobber). (b) `getDefaultProviderClient` decrypts the stored value before passing it to `NewOpenRouterClient`; empty key short-circuits the decrypt to avoid a "ciphertext too short" error path that admins can't act on (the client's own ErrApiKeyNotSet surfaces a clean "configure AI in settings" error instead). (c) `getSavedAPIKey` now decrypts both OpenAI and OpenRouter (was OpenAI-only) so TestProvider's "use saved key" path round-trips correctly. **v2 deltas vs v1.0.3:** **(i)** v1.0.3 91e0d610 was a tiny 10-line drive-by adding only the encrypt site; v2 also touches the read sides because the v2 port of T3b was internally consistent (plaintext store + plaintext consume), so flipping to encrypted required both halves. v1.0.3's read site was already trying to decrypt (see T3b note (iv) — the latent bug). **(ii)** No migration. v2's v2.2.16 seed ships `api_key: ""`, so there's no plaintext at rest on fresh installs. Devs who saved an OpenRouter key on the v2.1.1-plus-enhancements branch between T3b (acda6a6c) and T3j (484f7604) will see decrypt-failure → ErrApiKeyNotSet on next use and re-save, which writes a fresh encrypted value. Mirrors v1.0.3's no-migration approach. **(iii)** Stale comment cleanup: `queries.sql set-openrouter-config` and `migrations/v2.2.16.go` both referenced T3j as "future work" — updated to past tense. **Verified:** `go build ./...` clean, `go vet ./internal/ai/...` clean, `go test ./...` all packages pass. |
| T3k | Sanitise AI-generated HTML before editor insertion | `8487f74c` | S | T3a | done `9ee7a69c` | Frontend-only, applied at the only XSS surface in v2 (`ReplyBox.vue handleGenerateResponse`). Two-part change mirrored from v1.0.3 source: (a) `document.createElement('div').innerHTML` → `DOMParser.parseFromString(…, 'text/html')` for the conversation-transcript text-extraction loop — DOMParser produces an explicitly-detached document, sidestepping any browser-specific resource-preloading behaviour on the inert `<div>`. (b) Before assigning the LLM response to `htmlContent.value`, strip `<script>…</script>` blocks and `on*="…"` event-handler attributes via two regex replacements. **Bluemonday NOT reused here** despite SS4's `internal/stringutil/SanitizeHTML` being available: the AI output is intended as agent-editable rich text and `UGCPolicy` would strip formatting agents want to keep (lists, links, inline styling) before sending; the narrow `<script>` + `on*=` strip is the targeted-vector fix. **No new dep** — vanilla DOMParser + regex, identical library footprint to v1.0.3. **No backend change** — the RAG handler does not render output in any HTML-active context, so the assignment-site strip covers the surface. v1.0.3 source uses string-concat `'+'` and `replace(/\son\w+\s*=\s*…/gi, '')` verbatim; v2 port uses template-literal `${role}: …` to match v2's existing single-quote style but otherwise identical regexes. Test added zero unit tests in v1.0.3 either; verified via existing 130 frontend tests + go build/test green. |
| T3l | Prompt injection mitigation + O(n²) stripHTML fix | `7f73f8f5` | M | T3a | done `7536ca05` | Two fixes in the RAG layer. **(a) Prompt injection**: `defaultRAGSystemPrompt` in `cmd/rag.go` now wraps `{{enquiry}}` in `<customer_message>…</customer_message>` so the LLM treats the customer message as opaque user data rather than executable instructions. Mitigates "IGNORE ALL PREVIOUS INSTRUCTIONS"-style escapes. Custom admin-set prompts are not auto-wrapped — substitution is only at the default-template site; admins designing their own template can opt in. **(b) stripHTML**: `internal/rag/sync/macro.go` hoists the two regexes from per-call `MustCompile` to package-level vars (was recompiling on every macro + webpage-chunk during sync) and adds a 100KB input cap (`stripHTMLMaxLen`). New `TestStripHTMLCap` verifies the cap; new `BenchmarkStripHTML` measures throughput on 100KB dense HTML (7.7ms/op, 48 allocs on M1). v2 deltas: **(i)** v1.0.3 also wrapped the customer message in `classifySearchIntent`; v2 doesn't have `classifySearchIntent` (T3d external-search-API integration still pending), so only the one prompt site exists to fix here. **(ii)** v1.0.3's stripHTML lived in `cmd/rag.go` and was the buggy O(n²) loop-replace version; v2's equivalent in `internal/rag/sync/macro.go` was already regex-based and O(n) — the perf win here is package-level regex compilation + the input cap, not a loop rewrite. Existing nine `TestStripHTML` cases still pass unchanged. **(iii)** `stringutil.HTML2Text` via `k3a/html2text` is available in v2 but produces link-decoration output (`Click [https://…]`) that would break the existing test cases and change embedding inputs; not switched. **Verified**: `go build ./...` clean, `go test ./...` all pass, `pnpm exec vitest run` 130/130 pass. |

### 5.11 Tier 3 — Ecommerce (Maho/Magento)

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T3m | Ecommerce provider interface + models | `62f2bdb4` | M | – | done `1e8214f7` | Gateway commit for section 5.11. Two files: `internal/ecommerce/provider.go` (Provider interface — `Name`, `GetCustomerByEmail`, `GetOrdersByEmail`, `GetOrderByNumber`, `GetOrderByID`, `TestConnection` — plus `ErrNotFound`/`ErrUnauthorized` sentinel errors) and `internal/ecommerce/models.go` (Customer, Order, OrderItem, Address, Shipment, EcommerceContext, ProviderConfig). v2 deltas vs v1.0.3 62f2bdb4: **(i)** Both files import only stdlib (context, fmt, time) so the v2 module path is a no-op — file contents are byte-identical with v1.0.3 source. **(ii)** Spec-row description mentioned a `StatusEntry` struct; v1.0.3 source has no such struct — Order tracks status as a plain `string` field. Not adding one that the source doesn't have; downstream T3n/T3s can introduce it if needed. **(iii)** No tests in v1.0.3 either — T3m is interface scaffolding, tests come with implementations in T3n/T3o. **Verified:** `go build ./...` clean, `go vet ./...` clean, `go test ./...` all packages pass (ecommerce reports "no test files"), `pnpm exec vitest run` 130/130 pass. |
| T3n | Magento1/Maho provider implementation | `3e15a3f1` | L | T3m | pending | |
| T3o | Ecommerce manager — multi-stage context gathering | `285e9540` | M | T3m | pending | |
| T3p | Ecommerce settings admin UI | `810770de` | M | T3m | pending | |
| T3q | Ecommerce API handlers + routes | `80f808b0` | S | T3m | pending | Fold SS3 (`05823ba0`) in: `handleTestEcommerceConnection` must log full error server-side and return generic "Connection failed. Check your settings and try again." to the client. |
| T3r | Integrate ecommerce context into RAG | `bb7895b9` | M | T3a, T3m | pending | |
| T3s | Rewrite ecommerce for Maho Commerce API | `82dbff78` | L | T3m, T3n | pending | Replaces hash details — port end-state. |
| T3t | Ecommerce fixes batch | `583198db`, `f87c5593`, `bde3aaf6`, `4ac5f879`, `16427ec6`, `5706f936` | S | T3s | pending | Bundle into one fix commit. |
| T3u | Shipping context + SKU stock data for AI | `e918b386` (shipping portion), `d49a2851` (shipping portion), `cf97cb06` (SKU stock portion) | M | T3a, T3s | pending | |
| T3ae | Maho API Platform v2 migration: endpoints, OAuth body, 201 acceptance, UA tagging, lookup-failure UI surfacing | `b2882d00`, `eddada15`, `c42b470a` (ecommerce portion), `954797de`, `325c31de` | S | T3n, T3p, T3q, T3s | pending | Forward-port the iterative end-state of Maho integration after `82dbff78` (T3s baseline): **(a)** endpoint paths `/api/auth/token` → `/api/rest/v2/auth/token`, `/api/customers` → `/api/rest/v2/customers`, `/api/orders` → `/api/rest/v2/orders` in `magento1/{auth,client}.go`. **(b)** Accept HTTP 201 (in addition to 200) on token issuance — Maho's API Platform returns 201 Created. **(c)** Per-call User-Agent header `libredesk/<git-rev>` derived via `runtime/debug.ReadBuildInfo()` so libredesk traffic is identifiable in Maho's nginx logs (vs anonymous `Go-http-client/2.0`). Auth POST converted from `http.Post` to `http.NewRequest`+`http.Client.Do` so headers can be set. **(d)** `EcommerceContext.Warnings []string` collected per-stage in `manager.GatherFullContext` (auth, customer-by-email, orders-by-email, per-order Stage 3 lookups), deduplicated to avoid cascading-auth-failure noise; `gatherEcommerceContext` returns `(string, []string)`; rag handler envelope carries `ecommerce_warnings`; `ReplyBox.vue` toasts them as destructive variant after the success toast. **(e)** Admin Test Customer panel renders warnings in a red box (so 401s aren't masked as "no customer found"); Test Order panel already surfaces errors via `SendErrorEnvelope`. **(f)** `frontend/.../api/index.js` must export `testEcommerceCustomer` and `testEcommerceOrder` from the default object — they were defined but never exported on v1.0.3, tree-shaken from the build, lookup buttons silently noop'd; sanity-check this when porting. **(g)** One-time `sync.Once`-gated debug log of raw token response body so JWT field-name discrepancies are visible on first success. **(h)** JWT-shape regex fallback (`map[string]interface{}` scan for a 3-segment base64url string) if `tokenResp.Token` empty on decode — defensive against future field renames. **(i)** 1h default TTL when `expires_in` missing from response (common with JWT auth — without this fallback, every API call re-fetches the token because `tokenExpiry = now + (0-300)s = -5min`). **Auth body shape**: keep OAuth2 `client_credentials` grant (`grant_type`+`client_id`+`client_secret`), NOT the `customer` grant (`email`+`password`) — the latter is for human-user login on Maho's API platform; integrations use client_credentials. Confirmed live with `maho_…` token format that returned 201 + valid JWT. Land all of (a)–(i) atomically when porting T3n/T3o/T3p/T3q/T3s. |

### 5.12 Tier 3 — Voicemail transcription

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T3v | Voice transcription + audio player | subset of `13edaf3e` | L | – | done `8780d9ee` | Whisper API + on-ingest pipeline + admin settings + local-worker queue. v2 deltas: **(a)** AISettings model new in `internal/setting/models/models.go` (just `TranscriptionEnabled` + `TranscriptionProvider`; rest of v1.0.3's AI surface lives behind T3a and is out of scope). `setting.Manager.GetAISettings` mirrors `GetPCISettings`. **(b)** `internal/ai/transcribe.go` adds `(*OpenAIClient).TranscribeAudio` (multipart POST to `audio/transcriptions`, 120s timeout). `ai.Manager.GetOpenAIClient` exposes a nil-safe handle for the conversation pkg — Whisper isn't available on OpenRouter so the orchestration layer can't go through the default-provider machinery. **(c)** `internal/conversation/transcribe.go` ports the v1.0.3 orchestrator with two changes: `transcribeFunc == nil` is treated as a warn+skip (graceful no-op when AI isn't configured), and `insertTranscript` resolves the system user via `userStore.GetSystemUser()` instead of hard-coding `sender_id=1` (matches T3y's PCI activity-note pattern; the shell `transcribe-worker.sh` keeps id=1 for simplicity, documented in troubleshooting). **(d)** Hook lands in `ProcessIncomingMessage` after `InsertMessage` + `notifyParticipants`, gated on `len(msg.Media) > 0`. v2's flat `IncomingMessage` (no nested `.Message.Media`) means the call-site uses `msg.Media` after `uploadMessageAttachments` populates it. **(e)** `cmd/media.go` and `internal/media/media.go` add `audio/*` to the inline-disposition guards (both fs and s3 paths) so the in-thread `<audio>` player can stream. The audio player UI itself was already in v2 base (FS24 area). **(f)** Settings UI: new minimal `frontend/apps/main/src/views/admin/ai/AISettings.vue` mirroring `PCISettings.vue`'s shape — generic key/value form against `api.getSettings('ai')` + `api.updateSettings('ai')`; nav entry under `ai:manage` permission. v1.0.3's full RAG + knowledge-source AISettings page is T3a territory. **(g)** `transcribe-worker.sh` ships in repo root (operational tool, runs on host not in container); `docs/voice-transcription.md` ports normally — v2's `docs/` is committed (only `docs/superpowers` gitignored). **(h)** Migration `v2.2.14` + schema.sql baseline insert the two `ai.transcription_*` rows so fresh installs + upgrades converge — without them the plain-UPDATE settings statement silently no-ops on first save (same latent issue T3y dodges by never having actually been saved). **(i)** `cmd/main.go`: `initAI` hoisted out of the `App{}` literal so `TranscribeFunc` can be wired before `conversation.Run()` starts the workers, same shape as `IMAPUnspamFunc`/`IMAPDeleteFunc`. **(j)** `Sidebar.vue` `navIconMap` gains `Mic` + `ShieldAlert` — `ShieldAlert` was referenced in nav config since T3y but missing from the map, so this also fixes a latent missing-icon bug for the PCI nav entry. T3w + T3x deferred to their own units. Twilio webhook ingestion not added — T3v assumes audio attachments arrive via existing email/SMS channels; the pipeline activates once an audio attachment is on a message. |
| T3w | Voicemail transcription not triggering on new messages fix | `e4ea3891` | S | T3v | done `c6c69815` | One-line bug in T3v's `ProcessIncomingMessage` gate. `InsertMessage` uses `sqlx.Get` with `RETURNING *` against `conversation_messages` (which has no `attachments` column), and the StructScan path zeroes the in-memory `Media` slice that `uploadMessageAttachments` had populated moments earlier. The transcription gate `len(msg.Media) > 0` therefore always saw zero on fresh ingest and skipped. Fixed by gating on `len(msg.Attachments) > 0` (the parsed incoming-email payload, not touched by the DB scan) and refetching persisted media via `m.mediaStore.GetByModel(msg.ID, mmodels.ModelMessages)` before invoking `transcribeAudioAttachments`. v1.0.3 deltas: identical logic — v2's flat `msg` shape removes the `in.Message.*` indirection but the fix is the same shape. |
| T3x | Transcription debug logging + improvements | subset of `8f4ca805` | S | T3v | done `05690baa` | Five diagnostic log lines added to `internal/conversation/transcribe.go`: entry log on `transcribeAudioAttachments` (conversation_uuid + media_count), post-`GetAISettings` settings log (enabled + provider), `transcription is disabled, skipping` on the disabled-branch return, per-media `checking media for transcription` (uuid + content_type + is_audio) before the audioContentTypes filter, and post-write `transcription job queued` (uuid + job path) at the end of `transcribeViaLocal`. Also fixed a latent bug in `transcribeViaLocal`: WriteFile failure now early-returns instead of falling through to the success log. v1.0.3 source `8f4ca805` is a multi-feature checkpoint; filtered OUT of scope: cmd/rag.go `AgentInstructions` (T3a/RAG territory), ecommerce/manager.go context changes, pci_redact.go tweaks (T3y territory), ConversationFilterPanel/ConversationList tweaks (filter-panel scope), Dockerfile/docker-compose.yml infra bumps. Logger key/level naming follows T3v's structured-logger conventions. |

### 5.13 Tier 3 — PCI redaction

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T3y | PCI redaction (incoming + outgoing) | subset of `82e08d45` | M | – | done `9ebc62e6` | Use go-pci-scrub library. **Note**: the on-ingest `pciscrub` import + `FlagMessagePCI` query bindings + outgoing notification scrub were actually introduced in `d95049d4` (4h before `82e08d45`); 82e08d45 added the rest (pci_redact.go, imap_delete.go, schema, settings/handlers, frontend). Both commits are bundled into the T3y port. v1.0.3 used hardcoded `SenderID=1` for the system user in activity notes; v2 port resolves via `userStore.GetSystemUser()`. v1.0.3 hardcoded the mailbox to `INBOX` in the IMAP delete; v2 port adds a `pickPrimaryMailbox()` helper that walks the comma-separated list and skips spam/junk folders, mirroring the existing `pickSpamMailbox()` shape from `imap_unspam.go`. Migration `v2.2.12` adds `conversation_messages.has_pci_data` + `pci_detected_at` + partial index. |
| T3z | Per-inbox skip-PCI-scan toggle | `3f409266` | S | T3y | done `c0935e43` | Adds `SkipPCIScan` bool to `imodels.Config` (lives in the inbox `config` JSON column — no schema change), gates the on-ingest `pciscrub.ScrubWithSpans` call in `InsertMessage` on the per-inbox flag, surfaces a Switch toggle in `EmailInboxForm.vue` + `formSchema.js` + i18n keys `admin.inbox.skipPCIScan{,.description}`. v2 deltas vs v1.0.3: uses v2's `SwitchField` abstraction (single-component pattern) instead of v1.0.3's raw FormControl+Switch markup; uses i18n keys instead of hardcoded English strings. Outgoing notification scrub at `notifyParticipants` left unconditional, mirroring v1.0.3 (agents may legitimately type real card data; email-side leak is the bigger risk). The `SkipPCIScan` field also added to the local `currentCfg`/`updateCfg` structs in `inbox.Update` per the file's own warning comment ("any field on imodels.Config that should survive an Update MUST appear in both"). |
| T3aa | go-pci-scrub v0.3.0 word-boundary keyword matching | `4ecbf92f` | S | T3y | done `584241c7` | Bump dep + verify behaviour. v1.0.3 source went `v0.2.0 → v0.3.0`; v2 baseline (T3y) was `v0.1.0`, so port jumps `v0.1.0 → v0.3.0`. v1.0.3 commit was a pure go.mod/go.sum diff — no calling-code changes needed. T3y's existing PCI smoke test still passes against v0.3.0 (the new matching is strictly tighter, only reduces false positives). |

### 5.14 Tier 3 — Mobile push (FCM + Google mobile auth)

| ID | Feature | Source | Effort | Deps | Status | Notes |
|----|---------|--------|--------|------|--------|-------|
| T3ab | Push token register/unregister API | `cmd/push_tokens.go` portion of `d4f953b1` | S | – | done `5d5199c6` | Stores tokens per agent in `user_push_tokens` table (table name follows the v1.0.3 source — the spec's `push_tokens` was a paraphrase). New migration `v2.2.13` plus matching `schema.sql` baseline so fresh installs and upgrades converge — v1.0.3 never wrote the table into schema.sql, it was created ad hoc via psql in prod. v2 deltas vs v1.0.3: queries moved out of raw `app.db.Exec` into `internal/user/queries.sql` named-stmts (`register-push-token` / `unregister-push-token`) with `Manager.RegisterPushToken` / `Manager.UnregisterPushToken` wrappers, so we don't have to expose `db` on `App`. Error strings i18n'd (token-empty reuses `globals.messages.empty`, platform validation gets new key `user.invalidPushPlatform`). Routes mount at `/api/v1/agents/me/push-token` (POST/DELETE) — identical path to v1.0.3, so in-flight Flutter clients need no URL changes. |
| T3ac | Google mobile auth endpoint (Flutter OIDC → API key issuance) | `cmd/auth_mobile.go` portion of `d4f953b1` | M | – | done `d2860198` | Adds `POST /api/v1/auth/google-mobile`. Flutter app sends `{"id_token": "<google-jwt>"}`, server verifies signature against Google's JWKS, looks up an enabled agent by email, mints a fresh API key/secret pair (the existing v0.7.0 `users.api_key`/`api_secret` columns — no schema migration needed). v2 deltas vs v1.0.3: dropped `google.golang.org/api/idtoken` (would have been a new dep with its own un-guarded `http.Client`); reused the already-vendored `coreos/go-oidc/v3` via a new `auth.VerifyGoogleIDToken` method that lazily caches a Google verifier and routes both discovery and JWKS fetches through the existing SS2 SSRF-guarded client (`oidc.ClientContext` wrap). `SkipClientIDCheck=true` preserves v1.0.3's "any audience" semantics — the mobile app uses platform-specific Android/iOS Google client IDs and we don't want forks to enumerate them in config; signature-from-Google + `email_verified=true` + agent-must-exist-and-be-enabled is the security boundary. Agent lookup uses v2's generic `user.Get(0, email, [agent])` instead of v1.0.3's bespoke `GetAgent`. Error strings i18n'd (3 new keys: `auth.googleMobileTokenInvalid`, `auth.googleMobileNoAgent`, `auth.emailNotVerified`; reuses `globals.messages.empty`, `errors.parsingRequest`, `user.accountDisabled`). Route mounted at `/api/v1/auth/google-mobile` — identical path to v1.0.3, no Flutter URL changes. Wrapped in the `auth` rate-limit bucket like the other login routes. **No middleware change**: v2's `cmd/middlewares.go` already accepts `Authorization: Basic <key>:<secret>` (and `AuthToken`) via `ValidateAPIKey`, so the issued key works on every other endpoint immediately. **No schema migration**: API key columns already exist from v0.7.0 baseline; T3ac just exercises the existing `GenerateAPIKey` plumbing. Reload-time invalidation of the cached Google verifier when `allowed_hosts` changes (matches the OIDC verifier-rebuild logic). |
| T3ad | FCM sender + dispatcher hook | `internal/notification/fcm.go` portion of `d4f953b1` | M | T3ab | done `7976d59c` | Firebase Admin SDK (`firebase.google.com/go/v4 v4.19.0`); auto-removes invalid tokens (`messaging.IsUnregistered` / `messaging.IsInvalidArgument` → `DELETE FROM user_push_tokens`). Hook fires from `Dispatcher.sendToRecipient` after the in-app + WS broadcast, in a goroutine, gated on `d.fcm != nil`. v2 deltas vs v1.0.3: **(a)** push-token storage isolated behind `notifier.PushTokenStore` interface + `fcmPushTokenAdapter` in `cmd/init.go` that bridges `*user.Manager` — the v1.0.3 source reached straight into `*sqlx.DB` from the FCM file and added a `db` field on `App`; v2 keeps that pattern out, matching T3ab's "queries live in `internal/user/queries.sql` as named statements, not raw `db.Exec`". **(b)** new named statements `get-push-tokens` and `delete-push-token` added with matching `Manager.GetPushTokens` / `Manager.DeletePushToken` methods (delete kept distinct from T3ab's `unregister-push-token` because the semantics differ: logout vs invalidation, and one might gain logging later without the other). **(c)** service-account file path is config-driven (`notification.fcm.service_account_path`, default `/libredesk/firebase-service-account.json` to match the v1.0.3 docker-compose mount target) instead of v1.0.3's hardcoded path. **(d)** platform-config switch hoisted from v1.0.3's if/else-if chain to a `switch` (cosmetic). **NOT ported**: docker-compose.yml mount line. The v1.0.3 commit added `./firebase-service-account.json:/libredesk/firebase-service-account.json:ro` to its docker-compose.yml; the v2 upstream docker-compose.yml uses the official image and adding a non-existent bind-mount source would cause Docker to silently create a directory at that path, breaking startup for fresh installs that don't use FCM. The mageaustralia deploy infra already mounts the service-account JSON via its own deploy-time docker-compose; the upstream-derived file is left alone. The expected mount is documented in `[notification.fcm]` of `config.sample.toml`. |

## 6. Stop / resume protocol

**Within a session:**
1. Read this doc, find first unit with status `pending`.
2. Run the execution loop (Section 3).
3. Update status `pending → done <commit hash>`.
4. Repeat until either (a) Matthew says stop, (b) blocked on user input, or (c) hit a natural stopping point (theme boundary, end of Tier).

**At session end:**
1. Push branch: `git push mageaustralia v2.1.1-plus-enhancements`.
2. Append a checkpoint to the shared memory API:
   ```
   curl -X POST https://mem.mageaustralia.com.au/memory \
     -H "Authorization: Bearer USW8pKBPUTu6Y21a7wSWajB6YgufSw2ARGqG1qDnzx4" \
     -H "Content-Type: application/json" \
     -d '{"content":"v103-port: completed <unit IDs>. Next: <unit ID>", "source":"checkpoint-libredesk-port", "author":"claude-code"}'
   ```
3. Clear in-flight TodoWrite tasks.

**At session start:**
1. Read this doc — Section 5 status column tells you what's done.
2. Check shared memory: `curl ".../memory/search?q=v103-port&n=3"` for the most recent checkpoint to confirm.
3. Pick first `pending` unit (respecting deps), apply execution loop.

## 7. Out-of-scope

- **Pure docs / README updates** unless they describe a setting page or feature shipping in this port.
- **Pure deploy auto-commits** (the `deploy: 2026-XX-XX` series).
- **Catch-all checkpoint commits** like `3b435e7e all TW customizations as of 2026-03-31` — these have no porting unit; their contents are folded into the relevant feature units they actually touched.
- **Upstream-authored commits** (anyone other than `mcampbell@mageaustralia.com.au`) — those came from `abhinavxd` and are already in v2.x by definition.
- **Mobile-app code itself** — only the backend endpoints the Flutter app talks to are in scope (T3ab, T3ac, T3ad).

## 8. Risks & open questions

- **Audit drift.** Several rows note "verify if already in v2" (EC9, ER8, FS22, SS6, UX9, UX19). At port time, run `git log` on the destination to confirm. If already done, mark `done <hash>` without a new commit.
- **Sub-feature splitting.** Big bundled commits (`9dc758c6`, `d4f953b1`, `82e08d45`, `9513bd98`, `ab5044aa`, `e66e38bc`) need on-the-fly splitting into multiple port commits. Spec lists them under multiple unit IDs but the actual split happens at port time.
- **Conflicts with v2.x upstream.** If upstream has independently shipped a feature on `master` since the fork (e.g. `v2.2`), the port may collide. Resolution: prefer upstream's version when functionally equivalent; only port the fork's version if it adds something material.
- **`d83ef682` RAG core (XL).** This is a single foundation commit. May warrant a sub-design before porting — flag at port time.
- **Migration ordering.** Several units add DB tables/columns (RAG, ecommerce, voicemail, PCI, push_tokens, follower system, recent activities, macro attachments). Each gets its own migration file in `internal/migrations/v2.X.Y.go`. Pick fresh semver for each port, append to `migList` in `cmd/upgrade.go`.
